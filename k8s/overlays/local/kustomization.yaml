apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 항등(identity) 오버레이: 현재 Docker Desktop 로컬 검증 결과(host.docker.internal, :local 이미지)를
# 그대로 재현한다. patch 없음 — dev overlay와 대칭 구조 및 회귀 검증용.
resources:
  - ../../base

commonLabels:
  environment: local
