apiVersion: v1
kind: Secret
metadata:
  name: workspace-secret
  namespace: brainx
type: Opaque
stringData:
  # 32바이트 이상 실제 JWT 서명 시크릿. User/Workspace/Admin/MCP가 공통 검증하므로 같은 값을 사용해야 한다.
  JWT_SECRET: CHANGE_ME_AT_LEAST_32_BYTE_SECRET
  # Compose로 띄운 Neo4j의 실제 비밀번호.
  NEO4J_PASSWORD: CHANGE_ME
