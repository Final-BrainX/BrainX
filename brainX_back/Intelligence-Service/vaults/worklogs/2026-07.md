# 2026-07 Worklog

## 2026-07-08 12:50 - MCP agent note search and RAG answer backend

Done: Added agent-oriented note retrieval capabilities within `Intelligence-Service` and `Mcp-Service` only. Intelligence semantic search now accepts `searchMode` with backward-compatible `SEMANTIC` default, projection-backed `KEYWORD` search without embedding/LLM charge, and `HYBRID` merge behavior that marks overlapping note matches as `HYBRID`. Added internal MCP RAG endpoint `POST /internal/v1/intelligence/rag-answer`, default-model resolution, citations, token/charge metadata, service-token protection, and MCP-facing `brainx_ask_notes`. MCP `brainx_search_notes` now forwards optional `mode`, and `brainx_ask_notes` requires `notes:read` plus `ai:search`. No CLI, Gateway, Workspace-Service, codex plugin, SSOT, or README files were changed because the requested scope was limited to the two backend services.

Artifacts: `src/main/java/com/brainx/intelligence/exploration/domain/SearchMode.java`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteKeywordSearchPort.java`, `src/main/java/com/brainx/intelligence/exploration/application/usecase/ExplorationService.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapter.java`, `src/main/java/com/brainx/intelligence/chat/application/port/inbound/AskNotesUseCase.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/McpRagAnswerService.java`, `src/main/java/com/brainx/intelligence/chat/adapter/web/InternalRagAnswerController.java`, `../Mcp-Service/src/main/java/com/brainx/mcp/downstream/HttpIntelligenceSearchGateway.java`, `../Mcp-Service/src/main/java/com/brainx/mcp/downstream/IntelligenceSearchGateway.java`, `../Mcp-Service/src/main/java/com/brainx/mcp/tool/BrainxNoteTool.java`, and related unit/integration tests.

Verification: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.domain.ExplorationDomainTest --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --tests com.brainx.intelligence.exploration.adapter.web.ExplorationControllerTest --tests com.brainx.intelligence.chat.application.usecase.McpRagAnswerServiceTest --tests com.brainx.intelligence.chat.adapter.web.InternalRagAnswerControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --console=plain` passed. `.\gradlew.bat --no-daemon test --console=plain` passed for Intelligence-Service (Gradle reported all test tasks up-to-date after the successful run). In Mcp-Service, `.\gradlew.bat --no-daemon test --tests com.brainx.mcp.tool.BrainxNoteToolTest --tests com.brainx.mcp.tool.McpToolConfigTest --tests com.brainx.mcp.downstream.HttpIntelligenceSearchGatewayTest --console=plain` passed and `.\gradlew.bat --no-daemon test --console=plain` passed. SSOT/OpenAPI extraction and README sync are intentionally deferred by scope constraint.

## 2026-07-08 00:36 - 클러스터링 1단계 prompt/validation 개선

완료: 공개 REST/OpenAPI/AsyncAPI와 DB schema 변경 없이 AI 클러스터링 1단계 개선을 적용했다. 클러스터링 prompt에서 사용자 `writingStyle` 적용을 제거하고, system/user prompt에 모든 입력 `noteId`가 정확히 한 번 포함되어야 한다는 규칙과 `All input note IDs` 배열, `noteCount`/`softTarget` runtime instruction을 추가했다. `ClusterResponseParser`는 unknown/missing/duplicate noteId, empty cluster, `maxClusters` 초과를 더 이상 조용히 보정하지 않고 validation error로 처리한다. validation 실패 시 최대 1회 repair pass를 수행하고, repair 전 별도 `AI_CLUSTERING` entitlement check와 token usage 기록을 수행한다. repair 성공 시 repair run의 `llmRunId`로 job을 완료하고, repair 실패 시 `FAILED`로 저장하며 completed event를 발행하지 않는다.

아티팩트: `src/main/java/com/brainx/intelligence/clustering/application/usecase/ClusteringService.java`, `src/main/java/com/brainx/intelligence/clustering/application/usecase/ClusterResponseParser.java`, `src/test/java/com/brainx/intelligence/clustering/application/usecase/ClusteringServiceTest.java`, `src/test/java/com/brainx/intelligence/clustering/application/usecase/ClusterResponseParserTest.java`, `README.md`, `docs/technical/knowledge-structure-analysis.md`, `docs/technical/ai-feature-catalog.md`.

검증: `.\gradlew.bat --no-daemon test --tests "*ClusteringServiceTest" --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests "*Clustering*" --console=plain` 통과. `.\gradlew.bat --no-daemon compileJava --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-07 15:42 - Agent default documentGroupId Workspace 변환 보정

완료: Intelligence 내부 기본 문서 그룹 논리값 `default`를 Workspace-Service의 실제 workspace id처럼 전달하지 않도록 `ExternalWorkspaceNoteAdapter`에서 Agent bulk-create 요청 시 `default`를 `null`로 변환했다. custom documentGroupId는 그대로 전달해 비기본 Workspace 생성 위치를 보존하고, default thread는 Workspace-Service의 기존 default resolve 경로를 사용한다.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.workspace.ExternalWorkspaceNoteAdapterTest" --console=plain` 통과.

## 2026-07-05 18:24 - Graph 하드코딩 클러스터 카테고리 제거

완료: `/graph` 클러스터 패널에서 `CLUSTERS`와 `["ml", "read", "proj", "work", "life"]` 기반 고정 카테고리 목록을 제거했다. AI 클러스터 결과가 있으면 해당 결과만 표시하고, AI 클러스터가 없거나 비활성인 경로에서는 현재 노트에 실제로 존재하는 cluster id만 태그 기반 라벨/색상으로 파생해 표시한다. 공용 `resolveAiCluster` fallback도 고정 카테고리 매핑 대신 `미분류`로 정리했다. OpenAPI/AsyncAPI SSOT와 README 변경은 없다.

아티팩트: `../../brainx-next/lib/ai-cluster-projection.ts`, `../../brainx-next/components/graph-screen.tsx`.

검증: `brainx-next`에서 `npm run typecheck` 통과. `git -C ..\..\brainx-next diff --check` 통과(LF/CRLF 경고만 출력). `rg -F`로 `/graph`와 공용 helper에 `CLUSTERS`, `clusterById`, `기본 카테고리`, `카테고리 기준`, `클러스터 (카테고리)` 잔여 참조 없음 확인. 기존 Next dev server `http://127.0.0.1:3001/graph` HTTP 200 확인. 백엔드 계약/Java/설정 변경이 없어 Intelligence-Service Gradle 검증은 생략했다.

## 2026-07-05 18:11 - Home 주제지도 AI 클러스터링 연동

완료: `/home` 주제지도가 고정 `CLUSTERS`/`notes[].cluster` 기준만 쓰던 흐름을 기존 AI 클러스터 latest/job API 흐름과 맞췄다. AI 클러스터 projection helper를 공용화해 `/graph`와 `/home`이 같은 정규화, noteId 필터링, `미분류(ai-unassigned)` 투영 규칙을 사용한다. 홈 주제지도에는 latest 상태 조회, `AI 클러스터링`/`다시 분석` 버튼, loading/analyzing/stale/failed 안내, 분석 가능 노트 수 표시를 추가했고, AI 결과가 없거나 dev/mock/guest 경로에서는 기존 기본 분류 fallback을 유지한다. OpenAPI/AsyncAPI SSOT와 README는 이미 해당 endpoint가 있어 변경하지 않았다.

아티팩트: `../../brainx-next/lib/ai-cluster-projection.ts`, `../../brainx-next/components/home-screen.tsx`, `../../brainx-next/components/graph-screen.tsx`.

검증: `brainx-next`에서 `npm run typecheck` 통과. `git -C ..\..\brainx-next diff --check` 통과(LF/CRLF 경고만 출력). 기존 Next dev server `http://127.0.0.1:3001/home` HTTP 200 확인. 백엔드 계약/Java/설정 변경이 없어 Intelligence-Service Gradle 검증은 생략했다.

후속: code review에서 AI 클러스터가 6개이고 `미분류`가 count 상위 5개 밖으로 밀릴 때 홈 주제지도에서 빠질 수 있는 문제가 발견되어, AI 클러스터 모드에서는 `ai-unassigned`를 상위 표시 목록에 반드시 보존하도록 보정했다. `npm run typecheck`와 `git -C ..\..\brainx-next diff --check` 재통과.

## 2026-07-04 22:31 - 프로필 모달 문체 설정 UI

완료: `brainx-next` 공통 프로필 설정 모달에 `문체` 탭을 추가하고 `conversationTone`, `writingStyle` 두 축을 조회/저장하는 UI를 붙였다. 저장 시 UI가 관리하는 key만 갱신하고 빈 값은 제거하며, 기존 map의 알 수 없는 key는 보존한다. `assistanceStyle`은 프론트 UI와 payload에 포함하지 않는다.

아티팩트: `../../brainx-next/components/utility/account-settings-modal.tsx`, `../../brainx-next/components/workspace-shell.tsx`.

검증: `brainx-next`에서 `npm run typecheck` 통과. `rg -n 'assistanceStyle|StyleProfilePanel|문체' ../../brainx-next/components/utility/account-settings-modal.tsx ../../brainx-next/components/workspace-shell.tsx`로 `assistanceStyle` 잔여 참조 없음과 새 문체 탭/패널 반영을 확인했다.

## 2026-07-04 14:27 - Graph note detail 우측 컨트롤 숨김

완료: `/graph` 노트 상세 패널이 열려 있을 때 추천 버튼뿐 아니라 줌/레이아웃/그래픽 설정을 포함한 우측 컨트롤 컬럼 전체가 뒤에서 비쳐 보이지 않도록 `opacity-0` 및 `pointer-events-none` 상태로 숨기도록 수정했다. 상세 패널 표시 여부는 `noteDetailPanelVisible` 파생값으로 통일하고, 추천 선택 모드의 `선택 종료` 버튼 흐름은 유지했다. OpenAPI/AsyncAPI SSOT 및 README 변경은 없다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-04 11:44 - Graph AI 안내 문구 노출 범위 수정

완료: `brainx-next` 그래프 노트 변환에서 빈 summary fallback으로 `아직 처리되지 않았습니다. AI 기능이 제한됩니다.` 문구를 넣지 않도록 수정했다. 일반 그래프 hover tooltip과 우측 상세 패널은 summary가 있을 때만 AI 요약을 표시하고, 징검다리 개념 추천/AI 연결 추천 선택 모드에서 AI 색인이 준비되지 않은 노트에 hover할 때만 맥락별 안내 문구를 보여주도록 정리했다. OpenAPI/AsyncAPI SSOT 및 README 변경은 없다.

아티팩트: `../../brainx-next/lib/graph-api.ts`, `../../brainx-next/lib/workspace-live-notes.ts`, `../../brainx-next/components/graph-screen.tsx`

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `package.json`에 `lint` 스크립트가 없어 `npm run lint`는 실행하지 않았다. `rg "아직 처리되지 않았습니다|AI 기능이 제한됩니다|아직 AI 색인이 준비되지 않은 노트" ../../brainx-next`로 기존 문구 제거 및 새 문구 위치를 확인했다.

## 2026-07-01 03:36 - Graph AI 연결 추천 UI

완료: `brainx-next` `/graph`에 기존 징검다리 추천과 별도의 `AI 연결 추천` 선택 모드를 추가했다. 노드 클릭 또는 `Shift + 드래그`로 1~10개 source note를 선택하고, source별 `POST /api/v1/ai/link-suggestions` 호출 결과를 source 그룹으로 보여준다. 후보별 `수락`과 `전체 수락`은 Workspace `POST /api/v1/notes/{noteId}/links`를 호출해 실제 링크를 만들고, 성공 즉시 그래프 edge와 note links를 optimistic update한다. 공개 OpenAPI/AsyncAPI와 백엔드 로직은 변경하지 않았다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/workspace-api.ts`, `../../README.md`

검증: `npm run typecheck` 통과.

## 2026-07-05 23:59 - Graph link accept Redis draft overwrite 방지

완료: `/graph` AI 연결 추천 수락 시 Redis draft markdown을 canonical content 저장 입력으로 쓰지 않도록 수정했다. 수락 흐름은 항상 `getNote(sourceNote.id)`로 가져온 saved note body에 wiki-link를 적용하고, 저장 payload와 optimistic note 갱신도 saved source 기준 title/folder/version을 사용한다. stale draft가 최신 server version으로 저장되어 사용자 미저장 내용을 발행하거나 newer saved change를 덮는 위험을 제거했다. 공개 API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check -- brainx-next/components/graph-screen.tsx` 통과(LF/CRLF 경고만 출력).

## 2026-07-06 00:45 - 별도 Agent 탭 실험 API/UI

완료: 기존 `/chat` UI/API를 대체하지 않고 별도 `/agent` 탭과 `POST/GET /api/v1/ai/agent-threads`, `POST /api/v1/ai/agent-threads/{threadId}/messages` SSE, `approve/reject` action API를 추가했다. Agent thread/message/action은 별도 `intelligence_agent_*` 테이블에 저장하며, v1 tool은 `CREATE_NOTE`, `APPEND_NOTE_CONTENT`만 허용한다. LLM planner는 action을 제안만 하고, Workspace mutation은 사용자가 action card를 승인한 뒤 `WorkspaceNotePort`를 통해 bulk-create 또는 content patch `APPEND`로 실행한다. append는 Agent 전용 note source port로 user/documentGroup ownership을 확인하고 Workspace snapshot 최신 version을 baseVersion으로 사용한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/agent/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/agent/**`, `src/main/resources/db/migration/V20260706_01__create_agent_tables.sql`, `src/test/java/com/brainx/intelligence/agent/application/usecase/AgentServiceTest.java`, `../../brainx-next/app/(app)/agent/page.tsx`, `../../brainx-next/components/agent-screen.tsx`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `README.md`, `docs/technical/ai-feature-catalog.md`, `docs/technical/intelligence-operational-db-ddl.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.agent.application.usecase.AgentServiceTest" --tests "com.brainx.intelligence.architecture.ArchitectureTest" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost 연결 거부 shutdown 로그만 출력, Gradle exit code 0). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-01 05:44 - Chat 초안 제목 및 개인 노트 톤

완료: `/chat` 초안 계열 라우트(`COMPOSE`, `NOTE_ACTION`)의 LLM system prompt에 첫 줄 `# <title>` Markdown heading, 한 줄 공백 뒤 본문, 개인 노트 작성 톤 규칙을 추가했다. 일반 RAG Q&A에는 제목 강제가 섞이지 않도록 테스트를 보강했고, 프론트 저장 fallback은 heading/thread title이 없을 때 `AI 초안`으로 떨어지도록 조정했다. OpenAPI SSOT와 Intelligence 로컬 OpenAPI 슬라이스에는 초안 SSE delta가 제목 heading으로 시작하는 Markdown 문자열임을 반영했다. AsyncAPI 구조 변경은 없다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `../../brainx-next/components/chat-screen.tsx`, `../../README.md`

검증: 번들 Python으로 `scripts\extract_intelligence_openapi.py` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 문서 라우팅 검색 `rg "vaults/|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md` 통과.

## 2026-07-01 12:34 - Bridge wiki-link 보장

완료: `bridge-concepts` 후보의 `bridgeReason`이 입력 순서의 첫 두 source note title을 `[[노트 제목]]` 형식으로 포함하도록 prompt와 후처리를 보강했다. 3개 이상 note를 선택해도 실제 bridge note가 직접 wiki-link로 연결하는 원본 concept은 앞의 두 note로 제한하고, 나머지는 후보 생성 배경으로만 쓰도록 명시했다. `/graph`에서 징검다리 후보 저장 시 생성되는 note markdown도 `/notes/{id}` 링크 대신 앞의 두 source note를 `[[...]]` wiki link로 저장하도록 맞췄다.

아티팩트: `src/main/java/com/brainx/intelligence/connection/application/usecase/ConnectionService.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/connection/ConnectionQualityApplicationRunner.java`, `scripts/capture_connection_cli.py`, `../../brainx-next/components/graph-screen.tsx`, `docs/technical/connection-api.md`, `../../README.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest --tests com.brainx.intelligence.infrastructure.dev.connection.ConnectionQualityApplicationRunnerTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `npm run typecheck` 통과. 실제 provider 기반 `capture_connection_cli.py`는 `QDRANT_COLLECTION=brainx_note_search_voyage_1024`, `SERVER_PORT=0`으로 `20260701-bridge-wikilink-quality-port0` run을 실행해 link/bridge scenario 모두 통과했고, bridge recommendation 3개가 두 원본 wiki link를 포함했다. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-01 12:34 - Graph AI selection drag hit-test

완료: `/graph`의 `징검다리 개념 추천`과 `AI 연결 추천` 선택 모드에서 `Shift + 드래그` 범위가 React Flow DOM 박스/라벨 폭 기준으로 과하게 잡히던 문제를 보정했다. 드래그 중 선택 후보를 React Flow 기본 `Partial` selection 결과가 아니라 현재 selection rectangle을 flow 좌표로 변환한 뒤 노드 중심점이 rectangle 안에 있는지로 직접 계산한다. 드래그 시작 전 선택은 baseline으로 유지하고, 드래그 중 잠깐 잡힌 노드를 누적하지 않도록 현재 rectangle 결과로 replace한다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `localhost:3002/graph`에서 Playwright 일회성 확인으로 `징검다리 개념 추천`, `AI 연결 추천` 모두 같은 `Shift + 드래그` 범위가 `3`개 선택으로 들어가는 것을 확인.

## 2026-07-01 15:26 - AI usage recorder 중앙화

완료: Intelligence-Service의 `TokenUsageRecord` 생성 로직을 `AiUsageRecorder`로 중앙화했다. chat/embedding/raw usage 정규화, cached input, billable input, reasoning token, model catalog 기반 estimated cost 계산을 recorder가 맡고, 각 기능은 `userId`, `featureId`, `modelId`, `causationId`와 provider usage만 전달한다. stream 기반 `ChatService`는 provider usage가 없으므로 message 내부 추정 `ChatTokenUsage`만 유지하고 Commerce ledger 이벤트는 발행하지 않도록 정리했다. Commerce의 credit ledger/API 소유권은 그대로 유지한다.

아티팩트: `src/main/java/com/brainx/intelligence/shared/application/service/AiUsageRecorder.java`, `docs/technical/ai-model-pricing-and-usage.md`, usage 기록을 호출하는 AI usecase/adapter 테스트

검증: `.\gradlew.bat --no-daemon compileJava --console=plain` 통과. `.\gradlew.bat --no-daemon compileTestJava --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.shared.application.service.AiUsageRecorderTest --tests com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest --tests com.brainx.intelligence.assist.application.usecase.AssistServiceTest --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.clustering.application.usecase.ClusteringServiceTest --tests com.brainx.intelligence.insight.application.usecase.InsightServiceTest --tests com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest --tests com.brainx.intelligence.organization.application.usecase.OrganizationServiceTest --tests com.brainx.intelligence.autolink.application.usecase.NoteAutoLinkServiceTest --tests com.brainx.intelligence.infrastructure.vector.qdrant.QdrantNoteSearchIndexAdapterTest --tests com.brainx.intelligence.infrastructure.search.OpenAiExternalSearchAdapterTest --tests com.brainx.intelligence.infrastructure.search.ExternalSearchConfigurationTest --tests com.brainx.intelligence.infrastructure.dev.rag.SampleRagServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-01 16:46 - Intelligence JWT 인증 연결

완료: Intelligence-Service `/api/v1/**`에 User-Service/Gateway와 같은 `JWT_SECRET` 기반 HS256 Bearer access token 검증을 붙였다. `typ=access`, `exp`, `sub`를 검증해 Spring Security authentication name이 userId가 되도록 했고, 기존 local/dev-auth profile의 `dev-test-user` 및 `X-User-Id` 개발 편의는 유지했다. OpenAPI SSOT는 이미 `bearerAuth`를 요구하므로 계약 변경은 없다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/security/JwtTokenVerifier.java`, `src/main/java/com/brainx/intelligence/infrastructure/security/JwtAuthenticationFilter.java`, `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/infrastructure/security/JwtSecurityTokenVerifierTest.java`, `src/test/java/com/brainx/intelligence/SettingsControllerDefaultSecurityTest.java`, `src/test/java/com/brainx/intelligence/support/JwtTestTokens.java`, `README.md`

검증: `.\gradlew.bat --no-daemon test --tests "*Security*" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 문서 라우팅 검색 `rg "bearerAuth|JWT_SECRET|brainx.jwt.secret|vaults/|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md src/main/resources/application.yaml` 통과.

## 2026-07-01 17:10 - Chat usage event 및 IME 검색 가드

완료: `POST /api/v1/ai/chat-threads/{threadId}/messages`의 SSOT `x-produces-events`에 맞춰 실제 AI stream 성공 후 `rag-chat` `TokenUsageRecordedRequested`가 다시 발행되도록 `ChatService`에 `AiUsageRecorder` 호출을 복구했다. provider usage가 없는 stream 경로이므로 기존 `ChatTokenUsage` 추정값을 기록하며, AI 호출이 없는 fixed answer는 과금 이벤트를 발행하지 않는다. `brainx-next` 전역 검색 input은 IME 조합 중 `Enter`, `ArrowDown`, `ArrowUp`을 처리하지 않도록 `isComposing`/`keyCode=229` 가드를 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `../../brainx-next/components/workspace-shell.tsx`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.shared.application.service.AiUsageRecorderTest --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-01 17:43 - Workspace event projection 신규 처리 정상화

완료: Workspace internal snapshot API 인증 방식에 맞춰 `ExternalWorkspaceNoteAdapter`가 `Authorization: Bearer` 대신 `X-Service-Token` 헤더로 `BRAINX_WORKSPACE_SERVICE_TOKEN`을 보내도록 수정했다. Intelligence artifact에 잘못 포함된 `application.yml`의 `ingestion-service` 설정을 제거하고 `application.yaml`만 기준 설정으로 남겼다. AWS dev compose에는 방어적으로 `SPRING_APPLICATION_NAME=intelligence-service`와 `SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092`를 명시했다. 기존 Kafka offset/replay는 건드리지 않았고, 신규 이벤트 처리 안정화만 범위로 했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/workspace/ExternalWorkspaceNoteAdapter.java`, `src/test/java/com/brainx/intelligence/infrastructure/workspace/ExternalWorkspaceNoteAdapterTest.java`, `src/main/resources/application.yml`, `README.md`, `../../infra/aws-dev/deploy/docker-compose.yml`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.workspace.* --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `rg "name: ingestion-service|env/ingestion-service.env" brainX_back/Intelligence-Service/src/main/resources` no match 확인. dummy env로 `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 배포 smoke는 새 image/compose가 아직 배포되지 않아 실행하지 않았다.

## 2026-07-02 10:20 - Chat thread archive/delete UI/API

완료: public OpenAPI SSOT에 chat thread `status=active|archived` 목록 필터, `PATCH /api/v1/ai/chat-threads/{threadId}`, `DELETE /api/v1/ai/chat-threads/{threadId}`를 추가했다. Intelligence-Service는 `archived_at`, `deleted_at` soft state로 보관/숨김 삭제를 처리하고, deleted thread는 조회/목록/메시지 전송에서 제외하며 archived thread는 읽기만 허용하고 새 메시지는 409로 막는다. `/chat` 사이드바에는 `최근/보관` 필터, thread row action menu, 삭제 확인 dialog를 추가했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/lib/intelligence-api.ts`

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.chat.ChatJpaAdapterTest --tests com.brainx.intelligence.chat.adapter.web.ChatControllerTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과. 운영 DB는 `ddl-auto=validate`이므로 `intelligence_chat_threads.archived_at`, `intelligence_chat_threads.deleted_at` nullable `timestamp with time zone` 컬럼 추가 migration이 별도로 필요하다.

## 2026-07-02 10:45 - AWS dev chat thread archive/delete DDL 적용

완료: AWS dev RDS `brainx-dev-postgres`의 `brainx_intelligence` DB에 `intelligence_chat_threads.archived_at`, `intelligence_chat_threads.deleted_at` nullable `timestamp with time zone` 컬럼과 `idx_chat_threads_user_state_created` 인덱스를 적용했다. RDS는 private 접근 구조라 EC2 `i-0b5a622e94da9b41c`에서 SSM `AWS-RunShellScript`로 `psql`을 실행했다.

검증: 적용 전 컬럼 조회 결과가 비어 있음을 확인했고, 적용 후 `archived_at:timestamp with time zone:YES`, `deleted_at:timestamp with time zone:YES`, `idx_chat_threads_user_state_created` 조회를 확인했다. secret 값은 출력하지 않았다.

## 2026-07-02 12:30 - PR contracts CI 실패 원인 보정

완료: PR #111의 `ci / contracts` 실패 원인을 확인했다. 이번 PR에서 `contracts-v2`가 변경되면서 Redocly가 전체 OpenAPI SSOT를 lint했고, 기존 SSOT에 남아 있던 null-only `data` schema의 `type` 누락, `OnboardingCompleteRequest`/`GraphNodePosition` 누락 ref, `cancelMyDeletion` operation의 잘못된 path 위치가 error로 드러났다. SSOT에서 해당 error만 최소 보정하고 Intelligence local OpenAPI slice를 재생성했다.

검증: `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과. `npx --yes --cache %TEMP%\brainx-npx-asyncapi-cache @asyncapi/cli validate contracts-v2/brainx-asyncapi.ssot.yaml` 통과. `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `git diff --check` 통과.

## 2026-07-02 13:05 - PR #111 Codex review 반영

완료: PR #111 Codex review의 unresolved thread 4개를 반영했다. `POST /api/v1/ai/chat-threads/{threadId}/messages` SSOT에 archived/deleted thread 결과인 `404`, `409` 응답을 추가했고, 기존 `intelligence_chat_threads` table용 partial migration SQL을 operational DDL 문서에 명시했다. `/chat` UI는 보관 탭에서 새 대화를 시작한 뒤 stale closure가 `status=archived`로 목록을 다시 로드하지 않도록 `loadThreadPage(..., statusOverride)`를 추가했다. PR validation workflow는 backend service check job을 제거하고 contracts/frontend/admin/deploy/repository policy 중심으로 유지했다.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과. `npx --yes --cache %TEMP%\brainx-npx-asyncapi-cache @asyncapi/cli validate contracts-v2/brainx-asyncapi.ssot.yaml` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. backend check는 PR workflow에서 제외했다.
## 2026-07-02 14:37 - AI 클러스터 latest 및 Graph UI 연결

완료: AI 클러스터링을 사용자 요청 기반 지식 구조 스냅샷으로 쓰도록 `GET /api/v1/ai/clusters/latest`를 추가했다. `ClusterJobData`에 `documentGroupId`, `createdAt`, `completedAt`, `failureMessage`를 노출하고, POST 시 `scope_json._sourceSnapshot`에 noteId/updatedAt snapshot을 저장해 latest 조회에서 `NO_SOURCE_NOTES`, `NOT_ANALYZED`, `FRESH`, `STALE`, `FAILED` 상태를 계산한다. `_sourceSnapshot`은 public response와 `ClusterJobRequested` event scope에는 노출하지 않는다. `/graph` UI는 실제 로그인 + mock off 상태에서 latest를 조회하고, 사용자가 `AI 분석`/`다시 분석` 버튼을 누를 때만 cluster job을 요청하며, 완료된 AI cluster 결과를 동적 cluster 색상/필터/tooltip/선택 패널에 반영한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/clustering`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/clustering`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `docs/technical/knowledge-structure-analysis.md`

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 warning 43건 유지). `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.clustering.application.usecase.ClusteringServiceTest --tests com.brainx.intelligence.clustering.adapter.web.ClusteringControllerTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.clustering.ClusterJobJpaAdapterTest --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `npm run typecheck` 통과. `npm run build` 통과. `git diff --check` 통과. `http://localhost:3002/graph` HTTP 200 확인.

## 2026-07-02 16:39 - Chat AI 제목 생성 및 입력창 자동 확장

완료: `ChatThreadCreateRequest`에 optional `initialMessage`를 추가하고, `/chat` 첫 질문으로 새 스레드를 만들 때 Intelligence-Service가 `chat-thread-title` AI 호출로 20자 이하 한국어 주제 제목을 생성하도록 했다. 제목 생성 실패, 빈 응답, disabled, entitlement denied는 fallback title로 스레드를 계속 생성한다. `/chat` composer textarea는 `scrollHeight` 기반 auto-resize로 바꾸고 최대 `min(240px, 32svh)` 초과 시 내부 스크롤을 사용하도록 했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `docs/technical/rag-chat-api-frontend-integration.md`, `README.md`

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.chat.adapter.web.ChatControllerTest --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `npm run typecheck` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-02 22:14 - MCP 실제 노트 tool v1 구현

완료: Mcp-Service `/mcp` transport에 `brainx_search_notes`, `brainx_get_note`, `brainx_create_note` tool을 추가하고, API key scope guard를 붙였다. 검색은 Intelligence internal semantic search를 service token으로 호출하고, 읽기는 Workspace user-scoped note detail을 `X-User-Id`로 조회하며, 생성은 Workspace internal bulk-create를 `source=MCP_TOOL_CALL`로 호출한다. Intelligence-Service에는 `POST /internal/v1/intelligence/semantic-search`와 `X-Service-Token` 기반 internal auth를 추가했다. OpenAPI SSOT와 Intelligence local OpenAPI slice를 갱신했고, README에는 Codex Streamable HTTP 설정과 tool scope를 반영했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/exploration/adapter/web/ExplorationController.java`, `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `../Mcp-Service/src/main/java/com/brainx/mcp/tool`, `../Mcp-Service/src/main/java/com/brainx/mcp/downstream`, `../docker-compose.yml`, `../../README.md`

검증: `uv run scripts\extract_intelligence_openapi.py` 통과. `uv run scripts\extract_intelligence_related_contracts.py` 통과. `..\Mcp-Service\gradlew.bat --no-daemon check --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `docker compose config --quiet` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). AsyncAPI local slice는 이번 이벤트 계약 변경이 아니라 SSOT에 이미 있던 `WIKI` enum drift가 재생성 과정에서 반영됐다.

## 2026-07-03 00:13 - PR #125 Codex review MCP-only 반영

완료: PR #125 Codex review P1/P2를 Workspace-Service 변경 없이 MCP/compose 범위로 반영했다. 로컬 `apps` profile에 `Intelligence-Service`와 Qdrant를 추가해 `mcp-service`의 `INTELLIGENCE_SERVICE_URL=http://intelligence-service:8086`가 실제 Docker DNS로 해석되게 했고, AWS dev `mcp-service`에 `SERVICE_TOKEN`, `WORKSPACE_SERVICE_URL`, `INTELLIGENCE_SERVICE_URL`을 주입했다. `brainx_create_note`는 Workspace internal bulk-create 후 생성된 noteId를 `GET /api/v1/notes/{noteId}`로 다시 읽어 Workspace가 실제 저장한 dedupe title을 반환한다.

아티팩트: `../Mcp-Service/src/main/java/com/brainx/mcp/downstream/HttpWorkspaceNoteGateway.java`, `../Mcp-Service/src/test/java/com/brainx/mcp/downstream/HttpWorkspaceNoteGatewayTest.java`, `../docker-compose.yml`, `../../infra/aws-dev/deploy/docker-compose.yml`, `../../README.md`, `../.env.example`

검증: `..\Mcp-Service\gradlew.bat --no-daemon check --console=plain` 통과. `docker compose config --quiet` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). Workspace-Service `check`는 로컬 Neo4j 미실행으로 기존 통합 테스트가 실패해 중단했고, 사용자 요청에 따라 Workspace-Service/Workspace internal contract 변경은 제외했다.

## 2026-07-03 00:47 - MCP Codex 노트 tool scope 발급 UI 정합화

완료: `/settings` MCP API Keys 패널이 Codex 노트 도구용 key를 만들 때 `whoami`, `notes:read`, `ai:search`, `notes:write` 네 scope를 모두 보내도록 수정했다. 연결 정보에는 `brainx_whoami`, `brainx_search_notes`, `brainx_get_note`, `brainx_create_note` tool 목록을 안내하고, 발급 key 목록의 scope 배지는 긴 문자열도 넘치지 않게 정리했다. OpenAPI SSOT는 `ApiClientCreateRequest`/응답 scope 설명에 MCP v1 권한 의미를 보강했고, README에는 설정 화면 기본 발급 흐름을 반영했다. AsyncAPI 변경은 없다.

아티팩트: `../../brainx-next/components/utility/mcp-api-keys-panel.tsx`, `../../contracts-v2/brainx-openapi.ssot.yaml`, `../../README.md`

검증: `npm run typecheck` 통과. `npx --yes @redocly/cli lint contracts-v2\brainx-openapi.ssot.yaml` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-03 09:58 - PR #131 main 병합 충돌 해결

완료: `kyj` 브랜치에 `origin/main`을 병합하면서 MCP whoami v1과 MCP 노트 tool 확장 충돌을 정리했다. `X-Service-Token` 기반 internal semantic search 계약과 구현을 유지하고, Admin 모니터링/Grafana/배포 보정 변경은 함께 보존했다.

아티팩트: `../../README.md`, `../../contracts-v2/brainx-openapi.ssot.yaml`, `../Mcp-Service`, `../../brainx-next/components/utility/mcp-api-keys-panel.tsx`, `../../infra/aws-dev/deploy`

검증: `rg "^(<<<<<<<|=======|>>>>>>>)"` 충돌 marker 없음. `git diff --check` 통과. OpenAPI/AsyncAPI YAML 파싱 통과. `..\Mcp-Service\gradlew.bat --no-daemon test --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.adapter.web.ExplorationControllerTest --tests com.brainx.intelligence.SettingsControllerDefaultSecurityTest --tests com.brainx.intelligence.SettingsControllerLocalSecurityTest --console=plain` 통과. `..\Admin-Service\gradlew.bat --no-daemon test --tests com.brainx.admin.service.AdminServiceMonitoringSnapshotTest --console=plain` 통과. `npm run typecheck` 통과. AWS dev compose dummy env config 통과.

## 2026-07-03 12:01 - Intelligence note index retry worker

완료: `WorkspaceNoteEventHandler`의 snapshot 기반 색인 로직을 `NoteIndexingService`로 추출하고, 같은 로직을 쓰는 `ScheduledNoteReindexWorker`를 추가했다. worker는 `NOT_INDEXED`, `PROVISIONAL`, `STALE`, `FAILED`, `contentPending=true` projection 중 `nextIndexRetryAt`이 도래한 row를 batch로 처리하고, snapshot/Qdrant/embedding 실패를 retry metadata와 backoff로 기록한다. `NoteProjection`/JPA entity에는 `last_index_attempt_at`, `next_index_retry_at`, `index_attempt_count`, `last_index_error_code`, `last_index_error_message`를 추가했다. 전체 check를 막던 기존 `NoteIndexStatusService`의 application-to-infrastructure 의존은 outbound port로 분리했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/events/note`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteIndexStatusPort.java`, `docs/technical/note-chunking.md`, `docs/technical/intelligence-operational-db-ddl.md`

검증: 관련 테스트 `.\gradlew.bat --no-daemon test --tests "*WorkspaceNoteEventHandlerTest" --tests "*ScheduledNoteReindexWorkerTest" --tests "*NoteProjectionJpaAdapterTest" --tests "*SampleRagServiceTest" --tests "*FolderEventHandlerTest" --tests "*NoteIndexStatusServiceTest" --console=plain` 통과. 전체 `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 운영 DB는 `ddl-auto=validate`이므로 배포 전에 `intelligence_note_projections` retry metadata 컬럼과 `idx_note_projection_index_retry` 인덱스 추가 migration이 별도로 필요하다.

## 2026-07-03 12:25 - Chat SSE async dispatch security fix

완료: `/api/v1/ai/chat-threads/{threadId}/messages` SSE 응답 commit 이후 Spring MVC `ASYNC`/`ERROR` redispatch에서 Spring Security가 다시 인가를 걸어 스트림을 끊는 문제를 보정했다. `SecurityConfig`에서 `DispatcherType.ASYNC`, `DispatcherType.ERROR`를 `/internal/v1/**`, `/api/v1/**` rule보다 먼저 permit하도록 추가했다. 초기 request 인증 정책은 그대로 유지하고, 무인증 SSE POST가 계속 `401`을 반환하는 회귀 테스트를 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `src/test/java/com/brainx/intelligence/chat/adapter/web/ChatControllerTest.java`, `../../infra/aws-dev/troubleshooting.md`

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.adapter.web.ChatControllerTest --tests com.brainx.intelligence.SettingsControllerDefaultSecurityTest --console=plain` 통과. 첫 `clean check`는 184초 tool timeout으로 중단됐고, 재실행한 `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-03 13:20 - Note search index status frontend exposure

완료: public OpenAPI SSOT에 `POST /api/v1/intelligence/note-index-statuses`를 추가하고, note별 `searchIndexStatus`, `availableForAiFeatures`, `indexedAt`을 반환하도록 Intelligence-Service endpoint를 구현했다. projection이 없거나 조회 실패한 노트는 프론트에서 `UNKNOWN`/선택 불가로 처리하고, `/graph`의 AI 연결 추천, 징검다리 추천, AI 클러스터 분석은 `availableForAiFeatures=true`인 색인 완료 노트만 선택/실행 가능하게 제한했다. 노드 배지나 흐림 효과는 적용하지 않고, 패널 안내와 선택 제한만 둔다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/exploration`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapter.java`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/note-index-statuses.ts`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/components/planet-node.tsx`, `../../README.md`

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 MCP discovery 4XX warning 3건 유지). `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.application.usecase.NoteIndexStatusServiceTest --tests com.brainx.intelligence.exploration.adapter.web.ExplorationControllerTest --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-03 14:14 - PR #139 main 병합 충돌 해결

완료: `kyj` 브랜치에 `origin/main`을 병합하면서 PR #139의 충돌을 정리했다. MCP OAuth/API key fallback, MCP note tool scope UI, Intelligence SSE `DispatcherType.ASYNC/ERROR` 허용, note index status 프론트 fallback을 유지하고, `main`의 Workspace/Commerce/token usage/Kafka 변경도 함께 보존했다.

아티팩트: `../../README.md`, `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `../Mcp-Service/src/main/resources/application.yaml`, `../env/mcp-service.env.example`, `../../brainx-next/components/utility/mcp-api-keys-panel.tsx`, `../../infra/aws-dev/deploy/docker-compose.yml`

검증: `rg "^(<<<<<<<|=======|>>>>>>>)"` 충돌 marker 없음. unmerged 파일 없음. `git diff --check` 통과. SSOT OpenAPI/AsyncAPI YAML 파싱 통과. `docker compose --env-file <dummy> -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과. `brainX_back` local apps compose config는 임시 placeholder env 파일 생성/삭제 방식으로 통과. `npm run typecheck` 통과. `User-Service`, `Mcp-Service`, `Intelligence-Service` 각각 `.\gradlew.bat --no-daemon check --console=plain` 통과.

## 2026-07-03 17:51 - Intelligence-Service Flyway migration 도입

완료: 운영 DB schema drift로 `intelligence_note_projections.index_attempt_count` 컬럼이 없어 note projection 조회/재색인이 실패하던 문제를 Flyway migration 도입으로 복구 가능하게 했다. default profile은 Flyway enabled + `ddl-auto=validate`로 두고, H2 기반 `local`, `test`, `dev-ui` profile은 Flyway off + `create-drop`을 유지했다. 운영 baseline/repair migration은 `intelligence_note_projections` retry metadata, chat archive/delete/client context, AI model pricing 컬럼, note chunk/chat/cluster/insight table과 인덱스를 idempotent하게 보강한다. AI model catalog seed는 Flyway repeatable migration으로 이동했다. PR validate workflow에는 PostgreSQL service 기반 `ci / intelligence service` job을 추가했다.

아티팩트: `build.gradle`, `src/main/resources/application.yaml`, `src/main/resources/db/migration`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/FlywayPostgresMigrationTest.java`, `../../.github/workflows/brainx-pr-validate.yml`, `../../brainX_back/docker-compose.yml`, `../../infra/aws-dev/deploy/docker-compose.yml`, `docs/technical/intelligence-operational-db-ddl.md`

검증: `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과. `rg "^(<<<<<<<|=======|>>>>>>>)"` 충돌 marker 없음. `docker compose -f brainX_back/docker-compose.yml --profile apps config --quiet`는 임시 placeholder env 파일 생성/삭제 방식으로 통과. `docker compose --env-file <dummy> -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과. `docker compose -f brainX_back/Intelligence-Service/compose.yaml config --quiet` 통과. 로컬 Docker daemon과 localhost:5432 테스트 DB가 없어 PostgreSQL migration test의 실제 실행은 로컬에서 생략했고, GitHub Actions의 PostgreSQL service에서 실행되도록 추가했다.

## 2026-07-03 18:31 - PR #148 PostgreSQL Flyway validate CI 수정

완료: PR #148의 `ci / intelligence service`에서 Flyway migration 후 PostgreSQL `ddl-auto=validate`가 `@Lob` 필드와 운영 DDL의 `text` 컬럼 매핑 차이로 실패할 수 있어, JSON/본문/요약 계열 JPA 필드에 `@JdbcTypeCode(SqlTypes.LONGVARCHAR)`를 명시했다. 운영 DDL 문서와 migration은 그대로 `text` 기준을 유지한다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa`

검증: `.\gradlew.bat --no-daemon clean check --console=plain` 통과. 로컬 Docker daemon이 꺼져 있어 GitHub Actions PostgreSQL service 기반 `FlywayPostgresMigrationTest`의 실제 PostgreSQL 실행은 CI 재실행으로 확인한다.

## 2026-07-03 21:46 - PR #148 CI non-web migration test fix

완료: `FlywayPostgresMigrationTest`가 `spring.main.web-application-type=none`으로 앱을 띄울 때 servlet `HttpSecurity`가 없는 컨텍스트에서 `SecurityFilterChain` 빈을 만들려다 실패하던 문제를 수정했다. `SecurityConfig.securityFilterChain`을 servlet web application 조건에서만 생성하도록 제한했고, 사용하지 않는 Spring AI OpenAI embedding auto-config가 API key 없이 켜지지 않도록 기본 exclude 목록에 추가했다. REST/AsyncAPI SSOT 계약 변경은 없다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/security/SecurityConfig.java`, `src/main/resources/application.yaml`

검증: Docker PostgreSQL 테스트 DB와 `BRAINX_TEST_POSTGRES_URL` 계열 env로 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.FlywayPostgresMigrationTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. 종료 중 Eureka `localhost:8761` 연결 거부 로그는 있었지만 Gradle exit code 0.

## 2026-07-04 10:58 - Flyway legacy oid LOB repair

완료: AWS dev `brainx-intelligence-service`가 `V20260703_01__baseline_and_repair_intelligence_schema.sql` 실행 중 `trim(oid)`로 기동 실패하던 원인을 반영해, migration이 기존 Hibernate legacy `oid` LOB 컬럼을 `lo_get(oid)` 기반 UTF-8 `text`로 복구하도록 보강했다. 복구 실패 시 JSON/text 컬럼별 fallback을 적용하고, `trim(...)` backfill 조건은 명시적 text cast를 쓰도록 바꿨다. `FlywayPostgresMigrationTest`에는 legacy `oid` chat/note 컬럼을 만든 뒤 Flyway + JPA validate가 통과하는 PostgreSQL 재현 테스트를 추가했다. OpenAPI/AsyncAPI SSOT 변경은 없다.

아티팩트: `src/main/resources/db/migration/V20260703_01__baseline_and_repair_intelligence_schema.sql`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/FlywayPostgresMigrationTest.java`, `README.md`, `docs/technical/intelligence-operational-db-ddl.md`

검증: 로컬 Docker PostgreSQL(`postgres:16-alpine`, `brainx_intelligence_ci`)에서 `BRAINX_TEST_POSTGRES_URL=jdbc:postgresql://localhost:55432/brainx_intelligence_ci`로 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.FlywayPostgresMigrationTest --console=plain` 통과(2 tests, skipped 0). `.\gradlew.bat --no-daemon test --tests "*ScheduledNoteReindexWorkerTest" --tests "*NoteProjectionJpaAdapterTest" --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). AWS dev 복구 확인은 main 반영 후 `BrainX Dev Deploy` workflow를 `services=intelligence-service`로 실행한 뒤 SSM/health/public API로 확인해야 한다.

## 2026-07-04 19:00 - AI 채팅 초안 저장 제목 중복 제거

완료: `/chat`에서 AI 초안을 노트로 저장할 때 초안 첫 줄의 Markdown heading을 노트 제목으로 추출한 뒤에도 본문에 같은 H1이 남아 제목이 이중 표시되던 문제를 수정했다. 저장 직전 markdown 시작부 heading이 저장할 노트 제목과 같을 때만 해당 heading과 뒤따르는 빈 줄을 제거하며, 채팅 응답 렌더링과 백엔드 초안 프롬프트는 유지했다. OpenAPI/AsyncAPI SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/chat-screen.tsx`

검증: `npm run typecheck` 통과. `brainx-next`에는 `lint` 스크립트가 없어 lint는 실행하지 않았다.

## 2026-07-04 19:53 - AI 기능 카탈로그 문서화

완료: `Intelligence-Service`의 AI/LLM 기능을 공개 API, LLM 호출 여부, route/action, 분석 job, provider/model 설정, usage/event 경계 기준으로 정리한 기술 문서를 추가했다. 기존 세부 문서와 중복되지 않도록 전체 카탈로그 역할로 작성하고 `docs/technical/README.md`, `vaults/INDEX.md`에서 찾을 수 있게 연결했다. OpenAPI/AsyncAPI SSOT 변경은 없다.

아티팩트: `docs/technical/ai-feature-catalog.md`, `docs/technical/README.md`, `vaults/INDEX.md`

검증: 문서 전용 변경이므로 빌드/테스트는 실행하지 않았다. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-04 20:17 - 이어쓰기 인라인 미리보기 전환

완료: `/notes` 이어쓰기(CONTINUE) 결과를 별도 floating suggestion 창 대신 ProseMirror decoration 기반 본문 인라인 preview로 표시하도록 변경했다. preview는 저장 전 임시 DOM으로만 렌더링하고, 체크 버튼에서 기존 accept 삽입/ACCEPTED 기록을 수행하며, X 버튼에서 스트림 중단/REJECTED 기록을 유지한다. OpenAPI/AsyncAPI SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/notes/NoteEditor.tsx`, `../../brainx-next/app/globals.css`

검증: `brainx-next`에서 `npm run typecheck` 통과. 이후 체크 적용 텍스트가 Ctrl+Z 편집 히스토리에 들어가도록, 본문 삽입과 preview 제거를 하나의 TipTap transaction으로 묶고 `npm run typecheck` 재통과.

## 2026-07-04 21:06 - assistanceStyle 제거 및 StyleProfile prompt 적용

완료: 문체 프로필에서 `assistanceStyle`을 API 계약, domain/usecase/controller/JPA mapping에서 제거하고 `conversationTone`, `writingStyle` 두 축만 남겼다. `StylePromptCompiler`를 추가해 허용 key만 짧은 prompt instruction으로 변환하고, 대화형 응답/추천 이유에는 `conversationTone`, 생성/수정 결과물에는 `writingStyle`을 적용했다. 기존 `user_style_profiles.style` JSON text에서 legacy `assistanceStyle` key를 제거하는 PostgreSQL Flyway migration을 추가했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/settings/application/service/StylePromptCompiler.java`, settings/chat/assist/connection/autolink/organization/clustering/insight usecase, `src/main/resources/db/migration/V20260704_01__remove_assistance_style_from_style_profiles.sql`, 관련 테스트, `README.md`, `docs/domain/style-profile-input-direction.md`, `docs/technical/ai-feature-catalog.md`, `docs/technical/intelligence-operational-db-ddl.md`.

검증: `uv run scripts\extract_intelligence_openapi.py` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.settings.*" --tests "com.brainx.intelligence.chat.application.usecase.ChatServiceTest" --tests "com.brainx.intelligence.assist.application.usecase.AssistServiceTest" --tests "com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest" --tests "com.brainx.intelligence.autolink.application.usecase.NoteAutoLinkServiceTest" --tests "com.brainx.intelligence.organization.application.usecase.OrganizationServiceTest" --tests "com.brainx.intelligence.clustering.application.usecase.ClusteringServiceTest" --tests "com.brainx.intelligence.insight.application.usecase.InsightServiceTest" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost 연결 거부 shutdown 로그만 출력, Gradle exit code 0).

## 2026-07-04 21:40 - assistanceStyle 제거 후속 정리

완료: dev UI의 style profile 화면과 fixture에서 남아 있던 `assistanceStyle` 입력, 조회, 저장 경로를 제거하고 `conversationTone`, `writingStyle` 두 축만 사용하도록 맞췄다. `brainx-next` generated intelligence OpenAPI 타입을 최신 로컬 slice에서 재생성해 frontend contract에서도 `assistanceStyle`을 제거했다.

아티팩트: `src/devUi/java/com/brainx/intelligence/devui/DomainDevUiView.java`, `src/devUi/resources/dev-ui-fixtures/settings.json`, `../../brainx-next/lib/generated/intelligence-openapi.ts`.

검증: `.\gradlew.bat --no-daemon compileDevUiJava --console=plain` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `rg -n "assistanceStyle|AssistanceStyle" src/devUi ../../brainx-next/lib/generated/intelligence-openapi.ts`와 `rg -n "assistanceStyle|AssistanceStyle" ../../brainx-next -g "!node_modules" -g "!.next"`에서 남은 참조 없음. `git diff --check -- . ../../brainx-next/lib/generated/intelligence-openapi.ts` 통과(LF/CRLF 경고만 출력).

## 2026-07-04 23:06 - StyleProfile LLM 품질 평가

완료: `conversationTone`/`writingStyle` 두 축의 실제 LLM prompt 반영 품질을 평가하는 dev-only `style-profile-quality` runner와 capture script를 추가했다. runner는 stdin JSON 시나리오를 받아 StyleProfile을 저장한 뒤 conversation probe 또는 inline assist product path를 호출하고, deterministic checks와 실제 LLM judge 결과를 JSON으로 출력한다. capture script는 `bootJar`, jar 실행, preflight, raw stdout/stderr, `summary.json`, `responses.jsonl`, `report.md`를 `build/style-profile-quality-captures/20260704-style-profile-quality/`에 남긴다. SSOT/OpenAPI/AsyncAPI 변경은 없다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/dev/style/`, `src/test/java/com/brainx/intelligence/infrastructure/dev/style/`, `scripts/capture_style_profile_quality_cli.py`, `docs/technical/llm-quality-evaluations/style-profile-quality-evaluation-2026-07-04.md`, `docs/technical/llm-quality-evaluation-report-2026-07-04.md`, `docs/technical/README.md`.

실행 결과: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\capture_style_profile_quality_cli.py --run-name 20260704-style-profile-quality --timeout-seconds 600` 실행. preflight는 `.brainx-local.properties`, `OPENAI_API_KEY`, `SPRING_AI_MODEL_CHAT`, `OPENAI_CHAT_MODEL` 존재 확인만 기록했다. 실제 OpenAI chat model `gpt-5.4-mini` generation/judge 기준 5개 중 4개 통과, `writing-casual-light-rewrite` 1개는 `writingStyle` casual/light 반영이 약해 실패했다. 실패는 품질 개선점으로 보고서에 기록했다.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.dev.style.*" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost 연결 거부 shutdown 로그만 출력, Gradle exit code 0). `rg -n "assistanceStyle" src/main/java/com/brainx/intelligence/infrastructure/dev/style scripts/capture_style_profile_quality_cli.py docs/technical/llm-quality-evaluations/style-profile-quality-evaluation-2026-07-04.md`는 상세 보고서의 제외 범위 언급 1건만 출력. `git diff --check` 통과(LF/CRLF 경고만 출력). `rg -n "^(<<<<<<<|=======|>>>>>>>)"` 충돌 marker 없음.

## 2026-07-04 23:26 - StyleProfile 전체 자유 입력 전환

완료: `conversationTone`과 `writingStyle`의 세부 값들을 preset enum처럼 고정하지 않고 사용자가 임의 문자열로 입력/저장할 수 있게 정리했다. 프론트 프로필 모달의 문체 탭에서 기존 select 기반 항목을 text input으로 전환했고, `writingStyle.speechLevel`을 추가해 생성/수정 결과물 말투도 `해요체`, `합니다체`, `브랜드 톤`, `논문 초록처럼` 같은 자유 입력으로 받을 수 있게 했다. 빈 값은 기존 정책대로 해당 key를 삭제하고, UI가 모르는 기존 key는 보존한다. 백엔드 `StylePromptCompiler`와 style-profile 품질 평가 runner의 writing style allow-list에도 `speechLevel`을 추가했다. OpenAPI/AsyncAPI SSOT는 두 map 내부가 이미 open object라 변경하지 않았다.

아티팩트: `src/main/java/com/brainx/intelligence/settings/application/service/StylePromptCompiler.java`, `src/test/java/com/brainx/intelligence/settings/application/service/StylePromptCompilerTest.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/style/`, `src/test/java/com/brainx/intelligence/infrastructure/dev/style/`, `../../brainx-next/components/utility/account-settings-modal.tsx`, `scripts/capture_style_profile_quality_cli.py`, `README.md`, `docs/domain/style-profile-input-direction.md`, `docs/technical/ai-feature-catalog.md`.

검증: `npm run typecheck` 통과. `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile scripts\capture_style_profile_quality_cli.py` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.settings.application.service.StylePromptCompilerTest" --tests "com.brainx.intelligence.infrastructure.dev.style.*" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `assistanceStyle` 검색 결과는 제거 사유를 설명하는 문서 언급만 남았다.

## 2026-07-04 23:29 - StyleProfile preset + 직접 입력 UI 보정

완료: 프론트 문체 탭의 `conversationTone`/`writingStyle` 항목을 직접 입력만 가능한 형태에서 preset 선택과 직접 입력을 함께 지원하는 형태로 보정했다. 각 항목은 기존 추천값을 작은 선택 버튼으로 제공하고, 같은 행의 입력칸에서 임의 문자열을 그대로 수정할 수 있다. preset 선택 시 prompt에 더 자연스럽게 전달되도록 사람이 읽는 label 문자열을 저장하며, 기존 저장값이 old value code여도 선택 상태는 인식한다. OpenAPI/AsyncAPI SSOT와 백엔드 계약 변경은 없다.

아티팩트: `../../brainx-next/components/utility/account-settings-modal.tsx`.

검증: `npm run typecheck` 통과. `git diff --check -- ../../brainx-next/components/utility/account-settings-modal.tsx` 통과(LF/CRLF 경고만 출력).

## 2026-07-04 23:39 - StyleProfile 로드 실패 저장 방지

완료: 프론트 문체 탭에서 `getStyleProfile()` 로드가 실패했을 때 빈 draft/base 상태로 저장할 수 있던 경로를 막았다. 로드 실패 시 별도 오류 상태와 `다시 시도` 버튼을 보여주고, 저장 핸들러도 `loadError`가 남아 있으면 즉시 반환하도록 보강했다. 이로써 일시적인 네트워크/API 오류가 기존 `conversationTone`/`writingStyle` 값이나 UI가 모르는 key를 빈 payload로 덮어쓰는 위험을 줄였다. OpenAPI/AsyncAPI SSOT와 백엔드 계약 변경은 없다.

아티팩트: `../../brainx-next/components/utility/account-settings-modal.tsx`.

검증: `npm run typecheck` 통과. `git diff --check -- ../../brainx-next/components/utility/account-settings-modal.tsx vaults/worklogs/2026-07.md` 통과(LF/CRLF 경고만 출력).

## 2026-07-04 23:55 - StyleProfile dropdown 직접 입력 옵션 전환

완료: 프론트 문체 탭의 preset + 직접 입력 UI를 칩 버튼 방식에서 기존처럼 클릭하면 목록이 펼쳐지는 select/dropdown 방식으로 보정했다. 각 preset 항목은 dropdown option으로 노출하고, 같은 목록 안에 `직접 입력` option을 추가했다. 저장된 값이 preset label/value와 맞으면 dropdown에서 해당 preset을 선택 상태로 보여주고, `직접 입력`을 고르거나 저장된 값이 custom 문자열이면 입력칸을 열어 임의 문구를 편집할 수 있게 했다. 빈 값은 `선택 또는 직접 입력` 상태로 두어 불필요하게 모든 입력칸이 펼쳐지지 않게 했다. OpenAPI/AsyncAPI SSOT와 백엔드 계약 변경은 없다.

아티팩트: `../../brainx-next/components/utility/account-settings-modal.tsx`.

검증: `npm run typecheck` 통과. `git diff --check -- ../../brainx-next/components/utility/account-settings-modal.tsx vaults/worklogs/2026-07.md` 통과(LF/CRLF 경고만 출력).

## 2026-07-05 00:14 - StyleProfile prompt 강도 강화

완료: `StylePromptCompiler`가 `conversationTone`/`writingStyle`을 단순 key-value metadata가 아니라 `Mandatory user style instructions` 섹션으로 컴파일하도록 강화했다. 최종 사용자-facing 문장에는 문체를 반드시 적용하되 safety, factuality, evidence limit, output schema, 명시 사용자 지시가 충돌하면 우선하도록 계층형 guardrail을 추가했다. `writingStyle.speechLevel=음슴체`에는 한국어 출력에서 `함`, `임`, `됨`, `있음`, `없음` 계열 종결을 선호하고 `-요`, `-습니다`, `-합니다` 종결을 피하라는 전용 지시를 넣었다. call site 매핑은 기존 범위 그대로 유지했다.

아티팩트: `src/main/java/com/brainx/intelligence/settings/application/service/StylePromptCompiler.java`, 관련 chat/assist/connection/autolink/organization/clustering/insight usecase 테스트, `scripts/capture_style_profile_quality_cli.py`, `README.md`, `docs/domain/style-profile-input-direction.md`, `docs/technical/ai-feature-catalog.md`, `docs/technical/llm-quality-evaluations/style-profile-quality-evaluation-2026-07-04.md`, `docs/technical/llm-quality-evaluation-report-2026-07-04.md`.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.settings.application.service.StylePromptCompilerTest" --tests "com.brainx.intelligence.chat.application.usecase.ChatServiceTest" --tests "com.brainx.intelligence.assist.application.usecase.AssistServiceTest" --tests "com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest" --tests "com.brainx.intelligence.autolink.application.usecase.NoteAutoLinkServiceTest" --tests "com.brainx.intelligence.organization.application.usecase.OrganizationServiceTest" --tests "com.brainx.intelligence.clustering.application.usecase.ClusteringServiceTest" --tests "com.brainx.intelligence.insight.application.usecase.InsightServiceTest" --tests "com.brainx.intelligence.infrastructure.dev.style.*" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile scripts\capture_style_profile_quality_cli.py` 통과. `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\capture_style_profile_quality_cli.py --run-name 20260705-style-profile-strength --timeout-seconds 600` 실행 결과 실제 OpenAI chat model `gpt-5.4-mini` generation/judge 기준 5개 시나리오 모두 통과, capture artifact는 `build/style-profile-quality-captures/20260705-style-profile-strength/`에 저장. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-05 01:44 - StyleProfile dropdown 직접 입력 저장 보정

완료: 프론트 문체 탭의 dropdown + 직접 입력 UI에서 `직접 입력` option을 고를 때 기존 값을 즉시 지우지 않도록 보정했다. 기존 preset 값이나 저장된 값을 직접 입력으로 이어서 수정할 수 있게 했고, `haeyo`, `business` 같은 old preset value code가 로드되거나 저장될 때 사람이 읽는 label(`해요체`, `업무형` 등)로 정규화되도록 했다. OpenAPI/AsyncAPI SSOT와 백엔드 계약 변경은 없다.

아티팩트: `../../brainx-next/components/utility/account-settings-modal.tsx`.

검증: `npm run typecheck` 통과. `git diff --check -- ../../brainx-next/components/utility/account-settings-modal.tsx` 통과(LF/CRLF 경고만 출력).

## 2026-07-05 18:51 - Home AI 인사이트 리포트 API 연결

완료: `/home` AI 인사이트 패널을 실제 `POST /api/v1/ai/insight-reports` 생성 경로와 새 `GET /api/v1/ai/insight-reports/latest` 조회 경로에 연결했다. latest API는 `NO_SOURCE_NOTES`, `NOT_ANALYZED`, `FRESH`, `STALE`, `FAILED` 상태를 반환하고, user/document group 최신 workspace-scope report를 조회한다. 프론트는 `/home` 진입 시 latest만 조회하고, LLM 호출은 사용자가 버튼을 누를 때만 실행한다. 기존 로컬 계산 인사이트는 API 결과가 없을 때 fallback으로 유지했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/insight/**`, `src/main/resources/db/migration/V20260705_01__add_latest_insight_report_index.sql`, `../../brainx-next/components/home-screen.tsx`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `README.md`, `docs/technical/insight-reports.md`, `docs/technical/intelligence-operational-db-ddl.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests "*Insight*" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `docker compose up -d --build intelligence-service` 후 `GET /actuator/health` 200 `UP`, `GET /api/v1/ai/insight-reports/latest?documentGroupId=default` 200 `NO_SOURCE_NOTES` 응답 확인.

## 2026-07-05 19:14 - AI 링크 추천 LLM_ONLY 및 본문 wiki-link 저장

완료: public `POST /api/v1/ai/link-suggestions`가 `NoteAutoLinkUseCase`의 `LLM_ONLY` 전략을 사용하도록 바꾸고, suggestion 응답에 source markdown `anchorText`, `anchorStartOffset`, `anchorEndOffset`을 추가했다. `/graph`의 AI 연결 추천 수락은 기존 Workspace manual link command 대신 source note markdown을 `[[targetTitle|anchorText]]` 또는 안전 fallback `[[targetTitle]]`로 수정해 content save를 호출한다. Workspace의 기존 `syncWikiLinksForNote` 흐름이 `TYPE_WIKI` 링크와 graph projection을 생성한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/connection/**`, `src/test/java/com/brainx/intelligence/connection/**`, `src/test/java/com/brainx/intelligence/infrastructure/dev/connection/ConnectionQualityApplicationRunnerTest.java`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `README.md`, `docs/technical/connection-api.md`, `docs/technical/note-auto-linking.md`, `docs/technical/ai-feature-catalog.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests "*Connection*" --console=plain` 통과. `git diff --check -- <변경 범위>` 통과(LF/CRLF 경고만 출력).

## 2026-07-05 19:22 - Insight latest source snapshot 판정 보정

완료: `GET /api/v1/ai/insight-reports/latest`의 `FRESH/STALE` 판정을 단순 최신 수정 시간 비교에서 source note snapshot 비교로 바꿨다. insight report 저장 시 `scope._sourceSnapshot`에 source note id와 updatedAt을 저장하고, latest 조회 시 현재 searchable note set과 snapshot이 완전히 일치할 때만 `FRESH`를 반환한다. 삭제되거나 searchable 대상에서 빠진 노트, snapshot 없는 기존 report는 `STALE`로 판정한다. requested event scope에서는 internal snapshot key를 제거해 외부 payload에 노출하지 않는다.

아티팩트: `src/main/java/com/brainx/intelligence/insight/application/usecase/InsightService.java`, `src/test/java/com/brainx/intelligence/insight/application/usecase/InsightServiceTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.insight.application.usecase.InsightServiceTest" --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests "*Insight*" --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-05 23:17 - AI 연결 추천 수락 UX 개선

완료: `/graph` AI 연결 추천 패널에서 개별 수락과 전체 수락 중 `refresh` 아이콘에 회전 로딩 애니메이션을 표시하고, 개별 추천의 상태 문구를 본문 링크 저장 중임을 명확히 보이도록 보강했다. 수락 성공 시에는 연결 추천 패널을 자동으로 닫고, 실패 시에는 기존처럼 패널을 유지해 error state를 확인할 수 있게 했다. 공개 API/SSOT/백엔드 계약 변경은 없다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`.

검증: `npm run typecheck` 통과.

## 2026-07-05 23:28 - 링크 추천 source-only 분석 및 본문 부분 치환

완료: public `/api/v1/ai/link-suggestions`가 전체 노트 간 자동 링크를 계산한 뒤 source만 필터링하지 않고, 요청 source note 1개와 같은 `default` group의 후보 노트만 `LLM_ONLY`로 비교하도록 source-only 분석 경로를 추가했다. 후보가 없으면 provider 호출 없이 빈 결과를 반환한다. `/graph` 추천 수락은 Workspace draft를 우선 조회하고 없으면 note detail을 조회한 뒤 anchor offset 또는 유일 anchor 매칭이 확정될 때만 source 본문 일부를 `[[targetTitle|anchorText]]`로 치환하며, 위치를 확정할 수 없거나 본문 전체가 링크 하나로 바뀔 위험이 있으면 저장하지 않는다.

아티팩트: `src/main/java/com/brainx/intelligence/autolink/application/usecase/NoteAutoLinkService.java`, `src/main/java/com/brainx/intelligence/connection/application/usecase/ConnectionService.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapter.java`, `../../brainx-next/components/graph-screen.tsx`, `docs/technical/connection-api.md`, `docs/technical/note-auto-linking.md`, `README.md`.

검증: `.\gradlew.bat --no-daemon test --tests "*Connection*" --tests "*NoteAutoLink*" --console=plain` 통과. `npm run typecheck` 통과. Public OpenAPI/SSOT request/response schema 변경 없음.

## 2026-07-05 23:39 - 링크 추천 수락 draft version 리뷰 수정

완료: `/graph` AI 연결 추천 수락 시 draft markdown을 본문 소스로 우선 사용하더라도 저장 `baseVersion`은 draft의 `baseVersion`이 아니라 persisted note detail의 최신 `version`을 사용하도록 수정했다. stale draft baseVersion 때문에 정상 수락이 `NOTE_VERSION_CONFLICT`로 실패할 수 있는 회귀를 막는다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`.

검증: `npm run typecheck` 통과.

## 2026-07-05 23:54 - Intelligence-Service Redis 최소 설정 추가

완료: Intelligence-Service가 Redis 자동설정을 사용할 수 있도록 `spring-boot-starter-data-redis` 의존성과 `spring.data.redis.*` env 기반 설정을 추가했다. 기본 local 실행은 Redis health를 요구하지 않도록 두고, Intelligence-Service 전용 compose와 루트 `brainX_back/docker-compose.yml`에서는 `redis` health 의존성과 `BRAINX_REDIS_HEALTH_ENABLED=true`를 설정했다. 로컬 IDE 실행용 `env/intelligence-service.env`와 example에도 Redis host/port/timeout을 추가했다. 공개 API/SSOT 변경은 없다.

아티팩트: `build.gradle`, `src/main/resources/application.yaml`, `compose.yaml`, `../docker-compose.yml`, `../env/intelligence-service.env`, `../env/intelligence-service.env.example`, `README.md`.

검증: 기존 병합 충돌 파일들이 남아 있어 전체 Gradle 테스트는 실행하지 않았다. `docker compose config --quiet` 통과. `docker compose -f ../docker-compose.yml config --quiet` 통과. `git diff --check -- build.gradle src/main/resources/application.yaml compose.yaml ../docker-compose.yml ../env/intelligence-service.env ../env/intelligence-service.env.example README.md`는 변경 범위에서 LF/CRLF 경고만 출력.

## 2026-07-06 01:03 - Agent 리뷰 지적 3건 대응

완료: Agent action approve/reject가 pending 상태에서만 원자적으로 전이되도록 persistence port와 JPA 조건부 update를 추가해 중복 승인 실행을 막았다. `CREATE_NOTE` 실행 시 blank `targetFolderId`를 null로 정규화하고, Workspace bulk-create 요청 직전에도 한 번 더 방어했다. `/agent` 프론트는 기존 thread 전송 시 thread의 `modelId`를 사용하고, thread가 열린 동안 model picker를 해당 모델로 동기화/잠금 처리했다.

아티팩트: `src/main/java/com/brainx/intelligence/agent/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/agent/**`, `src/main/java/com/brainx/intelligence/infrastructure/workspace/ExternalWorkspaceNoteAdapter.java`, `src/test/java/com/brainx/intelligence/agent/**`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/agent/AgentJpaAdapterTest.java`, `../../brainx-next/components/agent-screen.tsx`.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.agent.*" --tests "com.brainx.intelligence.infrastructure.persistence.jpa.agent.AgentJpaAdapterTest" --console=plain` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력).

## 2026-07-06 01:45 - Cross-service integration 문서화

완료: `agent-documentation-compounder` 기준으로 root guide를 늘리지 않고, Intelligence-Service 내부 문서만 수정해 다른 BrainX 서비스와의 계약/이벤트/port 경계를 문서화했다. 새 `cross-service-integration-map.md`에는 Workspace/User/Gateway/Admin/MCP/Ingestion/Commerce 경계와 Discovery-Service 직접 coupling 없음, Agent v1의 승인형 Workspace mutation 및 note 조회/read-only tool 미지원 상태를 정리했다. 기존 vault routing, 기술 문서 index, 데이터 소유권 문서, consumed event domain map, AI feature catalog, domain implementation order, README의 낡은 package/Agent 설명도 현재 구현 기준으로 갱신했다.

아티팩트: `docs/technical/cross-service-integration-map.md`, `docs/technical/README.md`, `docs/technical/ai-feature-catalog.md`, `docs/domain/knowledge-intelligence-data-ownership.md`, `docs/domain/consumed-events-domain-map.md`, `vaults/agents/intelligence-service.md`, `vaults/agents/domain-implementation-order.md`, `vaults/INDEX.md`, `README.md`.

검증: 문서 전용 변경이므로 Gradle/npm 테스트는 생략. `git diff --check`, 변경 파일 경로 확인, `rg` 기반 라우팅/키워드/낡은 package 목록 검사를 통과했다.
## 2026-07-06 02:18 - Chat router 최신 주제 작성 요청 완화

완료: `/chat` 라우터가 최신 뉴스/스포츠/일반 웹 지식 주제를 언급하더라도 문서/보고서/글 초안 작성 deliverable이 명확하면 `COMPOSE`로 분류하도록 system prompt와 예시를 보강했다. 순수 최신 사실 조회는 계속 `OUT_OF_SCOPE`로 두며, `COMPOSE` 생성 prompt에는 노트 context나 검색 결과가 없을 때 실시간 검증을 주장하지 말라는 문구를 추가했다. 공개 API, AsyncAPI, 프론트엔드 변경은 없다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/LlmChatRouteDecider.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatRouteDeciderTest.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `docs/technical/llm-quality-evaluations/chat-router-quality-evaluation-2026-06-26.md`.

검증: `.\gradlew.bat --no-daemon test --rerun-tasks --tests com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-06 15:55 - Intelligence-Service LLMOps v1 기반 추가

완료: Intelligence-Service 전용 LLMOps v1 기반을 추가했다. `AiRunRecorder`가 생성형 provider 호출의 `llmRunId`, prompt key/version, preview, latency, token/cost, error를 `intelligence_llm_runs`에 저장하고, public `PUT /api/v1/ai/llm-feedback`는 `llmRunId` 기준 `LIKE`/`DISLIKE` 피드백을 upsert한다. internal `/internal/v1/intelligence/llmops/**` API로 run/feedback 조회, prompt definition/version 등록과 active 전환, eval set/scenario/run 동기 실행을 제공한다. Chat, inline assist, agent, bridge, folder organization, clustering, insight 흐름은 `llmRunId`를 응답에 포함할 수 있게 연결했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/llmops/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/llmops/**`, `src/main/resources/db/migration/V20260706_02__add_llmops_tables.sql`, `docs/technical/llmops.md`, `docs/technical/ai-feature-catalog.md`, `docs/technical/ai-model-pricing-and-usage.md`, `docs/technical/intelligence-operational-db-ddl.md`, `vaults/INDEX.md`.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.llmops.application.service.* --console=plain` 통과. 변경 영향 기능 테스트 묶음(Chat, Assist, Connection, Organization, Clustering, Insight, Agent) 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-06 16:42 - LLMOps 리뷰 지적 대응

완료: PromptOps DB active prompt가 켜져도 사용자 스타일, note sidebar guard, max cluster/recommendation/folder/move 제한 같은 런타임 지시가 사라지지 않도록 base prompt resolve 이후 동적 지시를 append하도록 보정했다. Agent entitlement 추정은 active `agent.planner` prompt 기준으로 계산하게 바꿨고, eval result는 성공/품질 실패/provider 실패 모두 실제 `llmRunId`를 저장하도록 수정했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java`, `src/main/java/com/brainx/intelligence/clustering/application/usecase/ClusteringService.java`, `src/main/java/com/brainx/intelligence/insight/application/usecase/InsightService.java`, `src/main/java/com/brainx/intelligence/organization/application/usecase/OrganizationService.java`, `src/main/java/com/brainx/intelligence/connection/application/usecase/ConnectionService.java`, `src/main/java/com/brainx/intelligence/agent/application/usecase/AgentService.java`, `src/main/java/com/brainx/intelligence/llmops/application/service/EvalRunnerService.java`, `src/test/java/com/brainx/intelligence/llmops/application/service/LlmOpsServiceTest.java`.

검증: `.\gradlew.bat --no-daemon compileJava --console=plain` 통과. `.\gradlew.bat --no-daemon testClasses --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.llmops.application.service.* --tests com.brainx.intelligence.agent.application.usecase.AgentServiceTest --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.assist.application.usecase.AssistServiceTest --tests com.brainx.intelligence.clustering.application.usecase.ClusteringServiceTest --tests com.brainx.intelligence.insight.application.usecase.InsightServiceTest --tests com.brainx.intelligence.organization.application.usecase.OrganizationServiceTest --tests com.brainx.intelligence.connection.application.usecase.ConnectionServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-06 17:30 - AI 챗 웹 검색 플래그 연동

완료: `/chat` 라우터 결과에 `requiresWebSearch`, `webSearchQuery`를 추가하고, `ChatService`가 검색 필요 요청에서 기존 `ExternalSearchPort`를 먼저 호출하도록 연결했다. 작성 요청은 `COMPOSE` route를 유지하면서 웹 컨텍스트를 초안 작성 근거로 사용하고, 순수 최신 사실 조회는 `OUT_OF_SCOPE + requiresWebSearch=true`일 때 웹 검색 답변으로 처리한다. provider 비활성/실패 시 최신 사실을 추측하지 않고 안내 답변을 저장한다. assistant message에는 노트 citation과 분리된 `webSources`를 저장하고, 프론트 `/chat`은 스트리밍 중 웹 확인 상태와 최종 웹 출처 섹션을 표시한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat/ChatMessageJpaEntity.java`, `src/main/resources/db/migration/V20260706_03__add_chat_web_sources.sql`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatRouteDeciderTest.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat/ChatJpaAdapterTest.java`, `docs/technical/external-search.md`, `docs/technical/intelligence-operational-db-ddl.md`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/components/chat/**`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --tests com.brainx.intelligence.infrastructure.persistence.jpa.chat.ChatJpaAdapterTest --console=plain` 통과(첫 시도는 Windows 파일 잠금으로 `build/test-results` 삭제 실패 후 재실행 통과). `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-06 17:52 - Chat AI 응답 피드백 UI 연결

완료: `/chat` assistant message에 `llmRunId`가 있는 경우 좋아요/싫어요 버튼을 표시하고 `PUT /api/v1/ai/llm-feedback`로 사용자별 feedback을 upsert하도록 연결했다. `ChatThreadDetailData.messages[]`를 `ChatMessageData` schema로 명시하고, chat detail 조회 시 현재 사용자의 `feedbackRating`을 병합해 새로고침 후에도 선택 상태가 복원되도록 했다. `llmRunId`가 없는 고정 응답, 에러 응답, 스트리밍 중 응답에는 feedback 버튼을 표시하지 않는다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/main/java/com/brainx/intelligence/llmops/**`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `src/test/java/com/brainx/intelligence/llmops/LlmOpsTestSupport.java`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/components/chat/**`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest --tests com.brainx.intelligence.chat.application.usecase.ChatServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).
## 2026-07-07 15:20 - Agent CREATE_NOTE documentGroupId 전달 보정

완료: Agent `CREATE_NOTE` 승인 실행이 thread/action의 `documentGroupId`를 Workspace internal bulk-create command에 전달하도록 수정했다. SSOT의 `/internal/v1/workspace/notes/bulk-create` consumer에 `internal.knowledge-intelligence`를 추가하고 `InternalNoteBulkCreateRequest.documentGroupId`를 optional 필드로 정의했다. Workspace-Service bulk-create는 이 값을 `NoteCreateRequest.documentGroupId`로 넘기며, 값이 없으면 기존처럼 default Workspace로 resolve한다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.consumed.openapi.yaml`, `src/main/resources/contracts/knowledge-intelligence.asyncapi.yaml`, `src/main/java/com/brainx/intelligence/infrastructure/workspace/ExternalWorkspaceNoteAdapter.java`, `src/test/java/com/brainx/intelligence/infrastructure/workspace/ExternalWorkspaceNoteAdapterTest.java`, `../Workspace-Service/src/main/java/com/brainx/workspace/dto/WorkspaceDtos.java`, `../Workspace-Service/src/main/java/com/brainx/workspace/service/WorkspaceService.java`, `../Workspace-Service/src/test/java/com/brainx/workspace/WorkspaceServiceCrudTests.java`, `README.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_related_contracts.py` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.workspace.ExternalWorkspaceNoteAdapterTest" --console=plain` 통과. Workspace-Service에서 `.\gradlew.bat --no-daemon test --tests "com.brainx.workspace.WorkspaceServiceCrudTests" --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-07 08:47 - Chat feedbackRating OpenAPI nullable lint 수정

완료: PR #186의 `ci / contracts` 실패 원인이던 `ChatMessageData.feedbackRating` nullable enum schema에 `type: string`을 추가했다. wire 의미는 `LlmFeedbackRating | null`로 유지하고, SSOT에서 Intelligence OpenAPI slice와 프론트 generated type을 재생성했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `../../brainx-next/lib/generated/intelligence-openapi.ts`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 4XX warning 11개만 출력). `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 로컬 `npx --yes @asyncapi/cli validate contracts-v2/brainx-asyncapi.ssot.yaml`는 변경 파일과 무관한 Windows npx cache `picomatch/lib/scan` module resolution 오류로 실행 실패.

## 2026-07-07 18:10 - Chat web search entitlement preflight

완료: 리뷰 지적대로 RAG chat router가 `requiresWebSearch=true`를 반환한 뒤 `ExternalSearchPort`를 호출하기 전에 `RAG_CHAT` entitlement preflight를 먼저 통과하도록 `ChatService` 순서를 보정했다. 고정 안내로 끝나는 out-of-scope/insufficient-context 경로는 기존처럼 provider 호출 없이 반환하고, 실제 외부 검색 또는 AI 호출 가능성이 있는 경로만 preflight gate를 지난다. 검색 결과를 포함한 최종 prompt 기준 entitlement check는 AI 호출 직전에도 유지해 기존 quota 방어를 보존했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "*ChatServiceTest" --console=plain` 통과. 공개 API/SSOT schema 변경 없음.

## 2026-07-07 09:55 - Intelligence-Service 핵심 리팩토링 및 agent 문서 정리

완료: 공개 REST/OpenAPI wire shape, AsyncAPI, env 변수, DB schema를 변경하지 않고 핵심 service/usecase의 내부 책임을 package-private collaborator로 분리했다. Chat은 entitlement guard와 web search resolver/context를 분리해 기존 web-search entitlement 순서를 유지했다. Security nested filter는 top-level filter로 이동했다. OpenAI external search adapter는 HTTP 호출, request factory, response mapper로 분리했다. AutoLink는 LLM JSON parser와 parser DTO를 분리했고, Connection link suggestion result mapping을 mapper로 분리했다. Agent planner JSON parser, Clustering cluster response parser, Insight response parser를 분리했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatEntitlementGuard.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatWebSearchResolver.java`, `src/main/java/com/brainx/intelligence/infrastructure/search/OpenAiExternalSearchRequestFactory.java`, `src/main/java/com/brainx/intelligence/infrastructure/search/OpenAiExternalSearchResponseMapper.java`, `src/main/java/com/brainx/intelligence/autolink/application/usecase/AutoLinkJsonParser.java`, `src/main/java/com/brainx/intelligence/connection/application/usecase/LinkSuggestionResultMapper.java`, `src/main/java/com/brainx/intelligence/agent/application/usecase/AgentPlanParser.java`, `src/main/java/com/brainx/intelligence/clustering/application/usecase/ClusterResponseParser.java`, `src/main/java/com/brainx/intelligence/insight/application/usecase/InsightResponseParser.java`, `vaults/agents/refactoring-guide.md`, `vaults/INDEX.md`, `vaults/agents/intelligence-service.md`, `vaults/workflows/verification.md`.

검증: `.\gradlew.bat --no-daemon compileJava --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests "*ChatServiceTest" --tests "*ChatRouteDeciderTest" --tests "*ChatControllerTest" --tests "*OpenAiExternalSearchAdapterTest" --tests "*ExternalSearchConfigurationTest" --tests "*NoteAutoLinkServiceTest" --tests "*ConnectionServiceTest" --tests "*AgentServiceTest" --tests "*ClusteringServiceTest" --tests "*InsightServiceTest" --tests "*ArchitectureTest" --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력). `rg "vaults/agents/refactoring-guide.md|vaults/workflows/verification.md|AGENTS.md|knowledge-intelligence.openapi.yaml|knowledge-intelligence.asyncapi.yaml|knowledge-intelligence.consumed.openapi.yaml" README.md AGENTS.md vaults src/main/resources/contracts/README.md` 통과. 계약 파일을 변경하지 않았으므로 OpenAPI/AsyncAPI extraction/codegen은 생략했다.

## 2026-07-07 18:32 - Chat web search unavailable non-draft route

완료: `COMPOSE` 또는 `NOTE_ACTION` route에서 웹 검색이 필요하지만 provider가 `none`이거나 실패해 안내 고정 응답을 반환할 때, stream route 이벤트를 `OUT_OF_SCOPE`로 내려보내도록 보정했다. `requiresWebSearch`와 `webSearchQuery`는 유지하되 route만 저장 불가 의미로 바꿔, 프론트가 "초안을 노트로 저장" 액션을 노출하지 않게 했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "*ChatServiceTest" --console=plain` 통과. 공개 API/SSOT schema 변경 없음.

## 2026-07-07 17:25 - Legacy default note projection 정리

완료: Workspace document group 도입 전 생성된 `document_group_id=default` Intelligence note projection이 현재 `dgrp_default_*` projection과 함께 남아 user-scope 조회에서 중복처럼 보이는 문제를 정리하도록 Flyway migration과 Qdrant cleanup runner를 추가했다. migration은 non-default 대체 projection이 있는 legacy default projection/chunk만 hard delete하고, Qdrant 삭제 대상은 `intelligence_legacy_default_document_group_repairs`에 기록한다. runner는 `BRAINX_REPAIR_LEGACY_DEFAULT_DOCUMENT_GROUP_VECTORS_ENABLED=true`일 때 기록된 대상의 legacy `default` vector를 best-effort로 삭제한다.

아티팩트: `src/main/resources/db/migration/V20260707_01__cleanup_legacy_default_note_projections.sql`, `src/main/java/com/brainx/intelligence/infrastructure/repair`, `src/test/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupVectorRepairRunnerTest.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/FlywayPostgresMigrationTest.java`, `src/main/resources/application.yaml`, `../env/intelligence-service.env.example`.

검증: `.\gradlew.bat --no-daemon compileJava compileTestJava --rerun-tasks --console=plain` 통과. `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.repair.LegacyDefaultDocumentGroupVectorRepairRunnerTest --console=plain` 통과. Docker PostgreSQL(`postgres:16-alpine`, `localhost:55439/brainx_intelligence_ci`)로 `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.FlywayPostgresMigrationTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 00:45 - AI Chat 웹검색 SSE timeout/UI 수정

완료: `/api/v1/ai/chat-threads/{threadId}/messages`가 thread 검증과 사용자 메시지 저장 후 즉시 `status(ROUTING)` SSE를 보내도록 수정했다. route 결정 후 웹 검색이 필요하면 `status(WEB_SEARCHING)`을 먼저 내려보내고, 검색 provider 실패/비활성은 HTTP 503이 아니라 fallback assistant delta/done으로 종료한다. Gateway에는 chat message SSE 전용 route를 generic Intelligence route보다 앞에 추가하고 per-route response-timeout을 120s로 늘렸다. 프론트 `/chat`은 새 `status` 이벤트를 받아 스트리밍 placeholder 아래에 `웹 검색 중…` UI를 표시한다.
추가로 SSE `error` 이벤트에 entitlement usage reason이 포함된 경우 기존 `AiUsageLimitExceededError`로 변환해 게스트/월간 한도 모달이 HTTP error 경로와 동일하게 표시되도록 보정했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat/application/port/inbound/SendChatMessageUseCase.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `../Gateway-Service/src/main/resources/application.yml`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/components/chat/ChatMessageItem.tsx`, `../../brainx-next/components/chat/types.ts`, `README.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.chat.application.usecase.ChatServiceTest" --tests "com.brainx.intelligence.chat.application.usecase.ChatRouteDeciderTest" --console=plain` 통과. `npm run typecheck` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 4XX warning 11개). `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet` 통과(secret env 미설정 warning만 출력). `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 01:59 - AI Chat 웹검색 streaming feedback 전환

완료: OpenAI external search adapter를 Responses API `stream=true` SSE 호출 기반으로 전환하고, `search_context_size=low`, timeout 기본 `60s`를 적용했다. `/chat` SSE는 검색 진행 `web_search_progress`, 검색 출처 `web_sources`, 답변 생성 `status.phase=ANSWERING`을 추가로 내려보내며, 프론트는 답변 완료 전에도 검색 중 상태와 출처 목록을 표시한다. 기존 `search(...)` 호출은 streaming 완료 이벤트를 모아 동일한 `ExternalSearchResponse`를 반환하도록 유지했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/shared/application/port/outbound/ExternalSearchPort.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatWebSearchResolver.java`, `src/main/java/com/brainx/intelligence/infrastructure/search/**`, `src/test/java/com/brainx/intelligence/infrastructure/search/OpenAiExternalSearchAdapterTest.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/components/chat/ChatMessageItem.tsx`, `../../brainx-next/components/chat/types.ts`, `README.md`, `docs/technical/external-search.md`, `docs/technical/ai-feature-catalog.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 4XX warning 11개). `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.search.OpenAiExternalSearchAdapterTest" --tests "com.brainx.intelligence.chat.application.usecase.ChatServiceTest" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `docker compose -f infra/aws-dev/deploy/docker-compose.yml config --quiet`, `docker compose -f brainX_back/docker-compose.yml config --quiet`, `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 02:33 - External Search streaming smoke CLI 보강

완료: `ExternalSearchApplicationRunner`에 `brainx.dev.external-search.stream-events=true` 모드를 추가해 `ExternalSearchPort.searchStream(...)`의 progress/source/completed 이벤트를 JSON `streamEvents`로 캡처할 수 있게 했다. `capture_external_search_cli.py`에는 `--stream-events` 옵션과 progress/source/completed 검증, report 요약 필드를 추가했다. capture 실행 시 기존 8086 포트 점유에 막히지 않도록 dev runner Java process에 `--server.port=0`을 넘기도록 했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/dev/search/ExternalSearchApplicationRunner.java`, `src/main/java/com/brainx/intelligence/infrastructure/dev/search/ExternalSearchDevProperties.java`, `src/test/java/com/brainx/intelligence/infrastructure/dev/search/ExternalSearchApplicationRunnerTest.java`, `scripts/capture_external_search_cli.py`, `docs/technical/external-search.md`.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.infrastructure.dev.search.ExternalSearchApplicationRunnerTest" --tests "com.brainx.intelligence.infrastructure.search.OpenAiExternalSearchAdapterTest" --console=plain` 통과. `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile scripts\capture_external_search_cli.py` 통과. 실제 OpenAI smoke `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\capture_external_search_cli.py --run-name 20260708-external-search-stream-smoke --stream-events --timeout-seconds 300` 통과. runId `20260708-external-search-stream-smoke`, model `gpt-5.5`, scenario 2개 모두 passed, sourceCount `[8,8]`, streamEventCount `[17,7]`, completedEvent 모두 true. 최초 실행은 `server.port=8086` 점유로 실패했고 `--server.port=0` 보강 후 재실행 통과.

## 2026-07-08 12:15 - AI Chat 초안 저장 상태 유지

완료: AI chat assistant message에 `route`와 `savedDraftNoteId`를 저장/응답하도록 SSOT, 도메인, JPA, 컨트롤러를 확장했다. `PUT /api/v1/ai/chat-threads/{threadId}/messages/{messageId}/draft-note`를 추가해 Workspace note 생성 후 메시지별 저장 noteId를 기록하며, 기존 매핑은 덮어쓰지 않고 그대로 반환한다. `/chat`은 thread 재조회 시 `route`로 "초안을 노트로 저장" 버튼을 복원하고, `savedDraftNoteId`가 있으면 "노트 열기" 상태를 유지한다. mapping 기록 실패 시에도 현재 세션의 local saved state는 생성된 noteId로 유지하도록 처리했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/chat/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat/**`, `src/main/resources/db/migration/V20260708_01__add_chat_message_route_and_saved_draft_note.sql`, `src/test/java/com/brainx/intelligence/chat/**`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/chat/ChatJpaAdapterTest.java`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/components/chat/**`, `../../brainx-next/lib/intelligence-api.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `README.md`, `docs/technical/rag-chat-api-frontend-integration.md`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.chat.application.usecase.ChatServiceTest" --tests "com.brainx.intelligence.chat.adapter.web.ChatControllerTest" --tests "com.brainx.intelligence.infrastructure.persistence.jpa.chat.ChatJpaAdapterTest" --console=plain` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown 로그만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력). 기본 `python`/`py` launcher는 PATH에 없어 번들 Python을 사용했다.

## 2026-07-08 14:06 - AI Chat draft route nullable contract lint 보정

완료: PR #216의 `ci / contracts` 실패 원인을 확인했다. `ChatMessageData.route`가 `nullable: true`와 `allOf`만 선언하고 `type`을 갖지 않아 Redocly `nullable-type-sibling` 규칙에서 실패했다. SSOT와 intelligence 로컬 OpenAPI slice에 `type: string`을 추가하고 frontend generated OpenAPI 타입을 재생성했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `../../brainx-next/lib/generated/intelligence-openapi.ts`.

검증: `C:\Users\fdsaf\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 4XX warning 11개만 출력). `npm run typecheck` 통과.

## 2026-07-08 14:25 - AI Chat draft duplicate note cleanup

완료: AI chat 초안 저장 시 다른 탭/오래된 세션에서 이미 같은 assistant message가 노트로 저장된 경우 중복 Workspace note가 남지 않도록 보정했다. 저장 직전 최신 thread를 조회해 기존 `savedDraftNoteId`가 있으면 새 노트를 만들지 않고 기존 노트로 이동한다. 생성 후 mapping 기록 응답이 방금 생성한 noteId와 다르면 backend가 기존 mapping을 반환한 것으로 보고 방금 만든 중복 노트를 permanent 삭제한다.

아티팩트: `../../brainx-next/components/chat-screen.tsx`.

검증: `npm run typecheck` 통과.

## 2026-07-08 14:43 - PR 216 리뷰 피드백 반영

완료: desktop vault local-only note ID가 서버 chat message의 `savedDraftNoteId`로 기록되지 않도록 프론트 저장 흐름을 보정했다. Workspace note 생성 결과에 `storage`, `remoteNoteId`를 보존하고, desktop vault에서는 synced `remoteNoteId`가 있을 때만 서버 mapping 기록을 시도한다. 운영 DDL 문서의 `intelligence_chat_messages` create/repair DDL에 `route`, `saved_draft_note_id` 컬럼도 추가했다.

아티팩트: `../../brainx-next/lib/workspace-api.ts`, `../../brainx-next/components/chat-screen.tsx`, `docs/technical/intelligence-operational-db-ddl.md`.

검증: `npm run typecheck` 통과. `git diff --check -- brainx-next/components/chat-screen.tsx brainx-next/lib/workspace-api.ts brainX_back/Intelligence-Service/docs/technical/intelligence-operational-db-ddl.md` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 14:40 - MCP search contract review fix

완료: 리뷰에서 지적된 계약 누락을 보정했다. Semantic search public/internal request schema에 `searchMode` enum(`SEMANTIC`, `KEYWORD`, `HYBRID`)을 추가했고, Mcp-Service가 호출하는 `/internal/v1/intelligence/rag-answer` service-token endpoint와 request/response schema를 SSOT에 추가했다. Intelligence 로컬 OpenAPI slice와 frontend generated OpenAPI 타입을 재생성했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `../../brainx-next/lib/generated/intelligence-openapi.ts`.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown WARN만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 15:08 - MCP RAG answer contract lint fix

완료: PR #221 `ci / contracts` 실패 원인을 수정했다. `RagAnswerData.tokenUsage`가 `nullable: true`와 `$ref allOf`를 쓰면서 sibling `type`이 없어 Redocly `nullable-type-sibling` rule에 실패했으므로, SSOT와 Intelligence OpenAPI slice에 `type: object`를 추가했다. frontend generated OpenAPI 타입은 재생성했지만 결과 diff는 없었다.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npx --yes @redocly/cli lint contracts-v2/brainx-openapi.ssot.yaml` 통과(기존 4xx warning만 출력).

## 2026-07-08 16:06 - Graph AI 색인 의존 제거

완료: `kyj`를 `origin/kyj` 최신으로 fast-forward한 뒤 `/graph` AI 기능의 source eligibility를 embedding index 완료가 아니라 active markdown projection 기준으로 분리했다. `availableForAiFeatures`는 graph AI source-ready 여부를 반환하고, `searchIndexStatus`는 기존 embedding index 상태를 그대로 노출한다. public `link-suggestions` source-only `LLM_ONLY`와 clustering request/latest는 `NOT_INDEXED`, `STALE`, `FAILED`여도 active + markdown + not pending이면 사용할 수 있게 했고, RAG/semantic search/keyword search/`VECTOR_LLM`/organization/insight 경로는 기존 indexed 조회를 유지했다. `/graph` UI의 색인 중심 문구도 AI 추천 가능/노트 동기화 기준으로 정리했다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/autolink/**`, `src/main/java/com/brainx/intelligence/clustering/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/**`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `README.md`, `docs/technical/connection-api.md`, `docs/technical/knowledge-structure-analysis.md`, `docs/technical/note-auto-linking.md`.

검증: `uv run --with pyyaml python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `.\gradlew.bat --no-daemon test --tests "*NoteProjectionJpaAdapterTest" --tests "*NoteIndexStatusServiceTest" --tests "*NoteAutoLinkServiceTest" --tests "*ClusteringServiceTest" --console=plain` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown WARN만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 16:20 - Workspace documentGroupId 기준 전환 및 legacy 색인 복구

완료: Intelligence-Service의 graph AI 기능 경로에서 `documentGroupId='default'`를 묵시 fallback으로 쓰던 흐름을 제거했다. 링크 추천, 징검다리 추천, 폴더 조직화, 클러스터/latest, 인사이트/latest, 색인 상태 조회가 요청/command의 명시 `documentGroupId`를 사용하게 했고, DOCUMENT_GROUP semantic search와 outbound retrieval query도 documentGroupId 누락을 거부하도록 보정했다. Workspace note/folder 이벤트 소비는 payload `documentGroupId`를 요구하게 했으며, Workspace-Service는 folder 이벤트, `NoteTagsChanged`, `/graph` ledger/Neo4j 노드에 `documentGroupId`를 포함하도록 맞췄다.

복구: `document_group_id='default'` legacy projection만 있고 현재 Workspace snapshot group projection이 없는 노트를 startup repair runner로 재색인 후 legacy projection/chunk/Qdrant point를 hard delete하는 idempotent runner를 추가했다. `BRAINX_REPAIR_LEGACY_DEFAULT_DOCUMENT_GROUP_BACKFILL_ENABLED`와 batch size env를 추가했고 AWS dev compose는 1회 복구를 위해 enabled로 둔다.

아티팩트: `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`, `src/main/java/com/brainx/intelligence/**`, `src/test/java/com/brainx/intelligence/**`, `../../brainX_back/Workspace-Service/src/main/java/com/brainx/workspace/**`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/lib/{graph-api,intelligence-api,note-index-statuses}.ts`, `../../brainx-next/lib/generated/intelligence-openapi.ts`, `../../brainX_back/docker-compose.yml`, `../../infra/aws-dev/deploy/docker-compose.yml`, `../../brainX_back/env/intelligence-service.env.example`.

검증: `python scripts\extract_intelligence_openapi.py` 통과. `npm run generate:intelligence-api-types` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain`(Intelligence-Service) 통과(Eureka localhost connection refused shutdown WARN만 출력). `.\gradlew.bat --no-daemon compileJava testClasses --console=plain`(Workspace-Service) 통과. `.\gradlew.bat --no-daemon test --tests "*WorkspaceServicePayloadHelperTest" --tests "*WorkspaceServiceDefaultWorkspaceTest" --console=plain` 통과. Workspace-Service 전체 `check`는 로컬에서 Gradle test worker가 7분 이상 종료되지 않아 중단했다. `git diff --check` 통과(LF/CRLF 경고만 출력). 충돌 marker scan 통과.

## 2026-07-08 16:34 - PR 225 review feedback fix

완료: PR #225 리뷰에서 지적된 두 건을 반영했다. MCP RAG answer는 Qdrant/embedding 검색을 호출하기 전에 모델 설정을 확인하고 보수적인 RAG_CHAT entitlement preflight를 먼저 수행하도록 변경했다. 실제 LLM 호출 직전의 최종 token estimate entitlement 검사는 유지했다. Graph AI source 조회 JPQL은 `search_index_status is null`인 legacy projection도 active markdown source이면 포함하도록 보정했다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/McpRagAnswerService.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaRepository.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/McpRagAnswerServiceTest.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/note/NoteProjectionJpaAdapterTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.chat.application.usecase.McpRagAnswerServiceTest" --tests "com.brainx.intelligence.infrastructure.persistence.jpa.note.NoteProjectionJpaAdapterTest" --console=plain` 통과.

## 2026-07-08 16:45 - Legacy backfill batch 리뷰 피드백 반영

완료: legacy `default` document group backfill runner가 startup 한 번에 batch 1회만 처리하던 문제를 고쳤다. 성공 대상은 삭제로 다음 조회에서 빠지고, 스킵/실패 대상은 현재 실행에서 blocked set으로 기억해 batch 뒤쪽의 복구 가능 대상을 계속 처리한다. 첫 대상이 스킵되어도 다음 대상까지 처리하는 회귀 테스트를 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunner.java`, `src/test/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunnerTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "*LegacyDefaultDocumentGroupBackfillRepairRunnerTest" --console=plain` 통과. `git diff --check -- brainX_back/Intelligence-Service/src/main/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunner.java brainX_back/Intelligence-Service/src/test/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunnerTest.java` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 17:36 - Legacy backfill event id 길이 초과 수정

완료: 운영에서 legacy `default` document group backfill이 `last_event_id varchar(160)` 초과로 실패하던 원인을 수정했다. repair runner의 synthetic event id를 raw `userId:noteId:documentGroupId` 조합 대신 `legacy-default-document-group-backfill:<sha256(scope)>` 고정 길이 형식으로 바꿨다. 긴 identifier 조합도 160자 안에 들어가고 같은 입력이면 같은 event id가 생성되는 회귀 테스트를 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunner.java`, `src/test/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunnerTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "*LegacyDefaultDocumentGroupBackfillRepairRunnerTest" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과(Eureka localhost connection refused shutdown WARN만 출력). `git diff --check -- brainX_back/Intelligence-Service/src/main/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunner.java brainX_back/Intelligence-Service/src/test/java/com/brainx/intelligence/infrastructure/repair/LegacyDefaultDocumentGroupBackfillRepairRunnerTest.java brainX_back/Intelligence-Service/vaults/worklogs/2026-07.md` 통과(LF/CRLF 경고만 출력).

## 2026-07-08 18:01 - kyj main 병합 충돌 해결

완료: `origin/main`을 `kyj`에 병합하면서 `ClusteringService`와 Intelligence worklog 충돌을 해소했다. 클러스터 latest 조회는 Workspace `documentGroupId` 명시 요구 정책을 유지했고, 양쪽 worklog 항목은 삭제 없이 보존했다.

검증: `rg -n "^(<<<<<<<|=======|>>>>>>>)"` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `.\gradlew.bat --no-daemon test --tests "*ClusteringServiceTest" --console=plain` 통과.

## 2026-07-08 18:34 - Gateway LLM route timeout 완화

완료: Gateway 전역 `response-timeout: 5s`가 동기 LLM 작업보다 먼저 끊겨 `intelligence is temporarily unavailable` fallback이 반복되는 문제를 완화했다. `intelligence-service-notes-summary`와 generic `intelligence-service` route에 route-level `response-timeout: 120000`을 추가하고, `intelligenceServiceCircuitBreaker` timelimiter를 `120s`로 늘렸다. README에는 일반 route는 짧은 timeout을 유지하되 LLM성 Intelligence route는 120초 기준을 쓰는 운영 원칙을 기록했다.

아티팩트: `../Gateway-Service/src/main/resources/application.yml`, `../../README.md`.

검증: OpenAPI/AsyncAPI 공개 계약 변경 없음. Gateway Spring configuration 변경 검증으로 `.\gradlew.bat --no-daemon check --console=plain`(Gateway-Service) 통과(Eureka localhost connection refused shutdown WARN/ERROR만 출력). `git diff --check -- README.md brainX_back/Gateway-Service/src/main/resources/application.yml brainX_back/Intelligence-Service/vaults/worklogs/2026-07.md` 통과(LF/CRLF 경고만 출력). Intelligence contract extraction/codegen은 계약 변경이 없어 생략했다.

## 2026-07-08 19:13 - PR 229 review feedback fix

완료: PR #229 리뷰 2건을 반영했다. `/notes` 우측 사이드바의 AI 링크 추천 수락은 서버 재조회 본문 대신 현재 active editor 본문을 flush/read해서 링크 적용 base로 사용하고, 저장 `baseVersion`도 active note version을 우선 사용해 미저장 편집 내용을 보존하면서 기존 version conflict 체크가 동작하도록 했다. Gateway는 `/api/v1/intelligence/**,/api/v1/ai/**` catch-all route의 120초 response-timeout을 제거하고, SSE/LLM 생성 route만 catch-all 앞에 별도 120초 route로 분리했다.

아티팩트: `../../brainx-next/components/notes/RightSidebar.tsx`, `../Gateway-Service/src/main/resources/application.yml`.

검증: `npm run typecheck` 통과. `.\gradlew.bat --no-daemon check --console=plain`(Gateway-Service) 통과(Eureka localhost connection refused shutdown WARN/ERROR만 출력). `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 00:23 - Home 인사이트 요약 레이아웃 안정화

완료: `/home`의 인사이트 요약 카드가 긴 AI 리포트 텍스트로 무한히 커지며 옆 레이아웃을 늘리는 문제를 줄였다. 2열 grid에 `items-start`/`minmax(0, ...)`/`min-w-0` 제약을 넣고, 인사이트 요약 카드 본문만 내부 스크롤되도록 `max-height`, `min-height: 0`, `overflow-y-auto`, 긴 문자열 wrap 처리를 적용했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check -- brainx-next/components/home-screen.tsx` 통과(LF/CRLF 경고만 출력).
## 2026-07-09 00:49 - AI 연결제안 동일 anchor 전체 치환

완료: `/notes`와 `/graph`가 공유하는 프론트 링크 추천 수락 유틸을 단일 위치 치환에서 안전한 모든 동일 문구 치환으로 바꿨다. 같은 `anchorText`가 여러 번 나오면 stale offset 여부와 관계없이 일반 본문 occurrence를 모두 같은 wiki-link로 바꾸고, fenced code, inline code, markdown link, 기존 wiki-link, HTML tag 내부는 제외한다. exact match가 없으면 case-insensitive, 공백 정규화 순으로 fallback하며, anchor가 사라졌거나 안전하지 않은 위치에만 있는 경우에는 더 구체적인 errorCode와 메시지를 반환한다. API/SSOT/DB 변경은 없다.

아티팩트: `../../brainx-next/lib/link-suggestions.ts`, `../../brainx-next/lib/link-suggestions.test.ts`.

검증: `node --test lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 01:08 - Home 인사이트 요약 최소 높이 보정

완료: `/home`의 인사이트 요약 카드가 내용이 적거나 없을 때도 일정한 공간을 유지하도록 outer container에 기본 `min-h-[min(320px,70vh)]`, 데스크톱 `lg:min-h-[420px]`를 추가했다. 기존 `max-h-[70vh]`, `lg:max-h-[620px]`, body `overflow-y-auto` 구조는 유지해 긴 리포트는 카드 내부에서만 스크롤된다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 01:31 - Home 레퍼런스 스타일 대시보드 리디자인

완료: `/home`을 레퍼런스 이미지에 가까운 넓은 반투명 캔버스형 지식 대시보드로 재구성했다. 상단은 날짜/Workspace pill/인사말과 AI 분석 status panel로 정리했고, KPI 4개는 metric card 형태로 바꿨다. 본문은 “나의 지식 인사이트”, “인사이트 요약”, “주제 지도”, “다시 보면 좋은 노트”의 기존 데이터 흐름과 버튼 동작을 유지하면서 카드 위계, spacing, scroll 제약, 모바일 wrap을 정리했다. 인사이트 요약 최소 높이는 작은 화면 상한과 충돌하지 않도록 `min-h-[min(320px,70vh)]`, `lg:min-h-[420px]`로 보정했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 01:36 - Home 대시보드 그라데이션 제거

완료: `/home` 리디자인에서 과하게 보이던 gradient background와 장식 별 요소를 제거하고, summary banner, 다시 볼 노트 callout, 상단 AI status panel을 flat `surface2`/border 기반 스타일로 정리했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `rg -n "gradient|✦|from-|via-|to-|transition-all" brainx-next/components/home-screen.tsx` 결과 없음.

## 2026-07-09 01:39 - Home 대시보드 흰색 surface 통일

완료: `/home`의 캔버스, KPI 카드, 인사이트/주제/추천 패널, 내부 summary/status 영역에서 `surface2` 또는 opacity가 섞인 배경을 제거하고 `bg-surface` 중심의 흰색 바탕으로 통일했다. 구분은 border, spacing, icon tint만으로 유지한다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `rg -n "bg-surface2|bg-surface/|bg-surface\\[|backdrop-blur|gradient|from-|via-|to-|transition-all" brainx-next/components/home-screen.tsx` 결과 없음.

## 2026-07-09 01:45 - Home 리뷰 피드백 대응

완료: `/home` 리디자인 리뷰에서 지적된 헤더 데이터 스코프와 hydration mismatch 가능성을 수정했다. 최근 활동 문구는 전역 `workspaceStats.activities` 대신 현재 Workspace로 필터된 `visibleNotes` 요약의 최신 노트만 사용한다. 날짜/시간대/인사말은 렌더 중 `new Date()`를 직접 계산하지 않고 mount 후 state로 설정해 서버/클라이언트 초기 출력 차이를 피한다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `rg -n "workspaceStats\\?\\.activities|const now = new Date\\(\\)|dateLabel = new Intl|dayPart = hour|greeting = hour" brainx-next/components/home-screen.tsx` 확인 결과 `new Date()`는 mount effect 내부에만 남음.

## 2026-07-09 03:06 - PR 236 링크 추천 리뷰 피드백 대응

완료: PR #236 Codex 리뷰 3건을 반영했다. link suggestion 전체 치환에서 HTML `<a>`, `<pre>`, `<code>` 등 protected element 본문을 제외하고, Latin anchor가 `JavaScript`처럼 더 큰 단어 내부에 있을 때는 치환하지 않도록 token boundary 검사를 추가했다. 공백 정규화 fallback은 빈 줄을 넘지 않고 inline whitespace 또는 single soft line break만 매칭하도록 제한했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/lib/link-suggestions.ts`, `../../brainx-next/lib/link-suggestions.test.ts`.

검증: `node --test lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과.

## 2026-07-09 03:37 - Home 대시보드 중앙 정렬 복구

완료: `/home` 리디자인 후 route 부모 폭에 따라 대시보드 캔버스 중앙 정렬이 깨질 수 있던 문제를 수정했다. route wrapper를 full-width flex container로 바꾸고 `justify-center`를 적용했으며, 내부 canvas는 `w-full max-w-[1360px]`로 제한해 사용 가능한 화면 폭 안에서 다시 중앙에 배치되도록 했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `Invoke-WebRequest http://localhost:3001/home` 200 확인. Edge headless 측정에서 route 내부 canvas 좌우 gutter가 각각 32px로 중앙 정렬됨을 확인.

## 2026-07-09 04:25 - Notes AI 연결제안 빈 에디터 fallback 수정

완료: `/notes` 오른쪽 AI 연결제안 수락 시 `activeEditor.getHTML()`이 빈 TipTap HTML이면 저장본 markdown으로 fallback하지 못해 anchor가 있는 본문에서도 "본문에서 연결할 위치를 찾지 못했어요"가 뜰 수 있던 문제를 수정했다. 빈 에디터 HTML 판정 helper와 fallback 선택 helper를 추가했고, 수락 저장 payload는 최신 저장본 version을 우선 사용하도록 정리했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/lib/link-suggestions.ts`, `../../brainx-next/lib/link-suggestions.test.ts`.

검증: `node --test lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 04:41 - Notes AI 연결제안 non-text editor content 보존

완료: 리뷰에서 지적된 빈 에디터 fallback 회귀를 보완했다. `linkSuggestionApplyContent`가 `<img>`/media/embed/table 같은 non-text HTML 노드를 실제 편집 내용으로 취급하게 하여, 텍스트가 없다는 이유만으로 최신 저장본 markdown fallback을 사용해 unsaved editor content를 덮지 않도록 했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/lib/link-suggestions.ts`, `../../brainx-next/lib/link-suggestions.test.ts`.

검증: `node --test lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과.

## 2026-07-09 05:07 - Chat 참조 근거노트 HTML 태그 노출 수정

완료: `/chat` 참조·근거 노트 패널에 TipTap HTML 태그가 plain text처럼 보이던 원인을 수정했다. 프론트 `stripMarkdown()`이 HTML comment/tag, `<br>`, 주요 HTML entity를 먼저 plain text로 정리한 뒤 기존 Markdown 정리를 수행하도록 바꿨고, Intelligence-Service `MarkdownNoteChunker`도 같은 방향으로 HTML 저장 본문을 정규화한다. chunk output 변경을 기존 색인 재처리 판정에 반영하기 위해 `CHUNKER_VERSION`을 2로 올렸다. API/SSOT/DB schema 변경은 없다.

아티팩트: `../../brainx-next/lib/utils.ts`, `../../brainx-next/lib/utils.test.ts`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunker.java`, `src/test/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunkerTest.java`.

검증: `node --test lib/utils.test.ts lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests "*MarkdownNoteChunkerTest" --console=plain` 통과.

## 2026-07-09 05:23 - Link suggestion custom asset block fallback 보정

완료: `/notes` AI 연결제안 수락 시 에디터 본문이 `data-image-block`, `data-pdf-block`, `data-ppt-block`, `data-html-block`만 포함한 asset-only HTML이면 빈 본문으로 오판해 저장본 markdown으로 fallback할 수 있던 문제를 수정했다. 커스텀 asset block 속성을 meaningful editor content로 취급해 unsaved asset-only content를 보존하고, 해당 회귀 테스트를 추가했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/lib/link-suggestions.ts`, `../../brainx-next/lib/link-suggestions.test.ts`.

검증: `node --test lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과.

## 2026-07-09 05:24 - HTML plain text angle bracket 보존 보정

완료: 리뷰에서 지적된 HTML 정규화 회귀를 보정했다. 프론트 `stripMarkdown()`과 백엔드 `MarkdownNoteChunker`의 HTML tag 제거 정규식을 태그 이름이 있는 형태로 좁혀 `A &lt; B &amp; C &gt; D` 같은 escaped 일반 텍스트가 통째로 제거되지 않도록 했다. 실제/escaped HTML 태그 제거 동작은 유지하고, angle bracket 일반 텍스트 보존 회귀 테스트를 추가했다. API/SSOT/DB schema 변경은 없다.

아티팩트: `../../brainx-next/lib/utils.ts`, `../../brainx-next/lib/utils.test.ts`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunker.java`, `src/test/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunkerTest.java`.

검증: `node --test lib/utils.test.ts lib/link-suggestions.test.ts` 통과. `.\gradlew.bat --no-daemon test --tests "*MarkdownNoteChunkerTest" --console=plain` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 05:44 - Home dashboard 중앙 정렬 회귀 수정

완료: `/home` 페이지가 `home.module.css`의 예전 polish wrapper를 통해 `[data-route]`에 `max-width: 1180px`를 덮어써 중앙 정렬이 깨지던 문제를 수정했다. `HomePage`에서 CSS module wrapper를 제거하고, 더 이상 참조되지 않는 `home.module.css`를 삭제해 `HomeScreen` 자체의 `w-full`, `justify-center`, `max-w-[1360px]` 정렬이 그대로 적용되도록 했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/app/(app)/home/page.tsx`, `../../brainx-next/app/(app)/home/home.module.css`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). Playwright headless 측정에서 1536px viewport 기준 canvas 좌우 route gutter가 각각 60px로 중앙 정렬됨을 확인.

## 2026-07-09 05:52 - Home dashboard 4분면 카드 높이 고정

완료: `/home` 본문 4개 대시보드 카드가 콘텐츠 높이에 따라 어긋나던 문제를 수정했다. desktop `xl` 이상에서 2열 grid를 동일 폭 column과 `460px` row로 고정하고, 네 section을 `xl:h-full xl:min-h-0`로 맞췄다. 각 카드 body는 `min-h-0`과 내부 scroll을 갖게 해 긴 콘텐츠가 카드 바깥으로 밀지 않게 했다. 모바일/태블릿은 기존처럼 한 열 흐름을 유지한다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). Playwright headless desktop 측정에서 4개 section 모두 `629x460`, top row/bottom row 정렬 일치 확인. 모바일 390px viewport에서 horizontal overflow 0 확인.

## 2026-07-09 05:58 - Home dashboard white canvas dark mode 보정

완료: `/home` outer canvas를 `bg-white`로 고정한 뒤 다크 테마에서 내부 `text-txt` 계열이 밝은 색으로 남아 흰 배경과 충돌할 수 있던 리뷰 지적을 반영했다. canvas 내부에 라이트 색상 토큰(`--surface`, `--surface2`, `--txt`, `--txt2`, `--txt3`, `--border`)을 스코프해 카드 배경과 텍스트가 함께 밝은 대시보드 톤으로 렌더링되게 했다. API/SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/home-screen.tsx`.

검증: `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 05:59 - HTML decoded generic text 보존 보정

완료: HTML 정규화의 decoded pass가 `x&lt;y&gt;z`, `List&lt;T&gt;`처럼 공백 없는 angle-bracket 일반 텍스트를 실제 HTML 태그처럼 삭제할 수 있던 리뷰 지적을 반영했다. 프론트 `stripMarkdown()`과 백엔드 `MarkdownNoteChunker`의 HTML tag 제거 패턴을 실제 HTML tag name whitelist 기반으로 좁혀 `<p>`, `<strong>`, `<div>` 등은 제거하되 `<y>`, `<T>` 같은 사용자 텍스트는 보존한다. API/SSOT/DB schema 변경은 없다.

아티팩트: `../../brainx-next/lib/utils.ts`, `../../brainx-next/lib/utils.test.ts`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunker.java`, `src/test/java/com/brainx/intelligence/infrastructure/events/note/MarkdownNoteChunkerTest.java`.

검증: `node --test lib/utils.test.ts` 통과. `.\gradlew.bat --no-daemon test --tests "*MarkdownNoteChunkerTest" --console=plain` 통과. `npm run typecheck` 통과.
## 2026-07-09 06:28 - AI 연결 추천 대괄호 제목 위키링크 보정

완료: AI 연결 추천 수락 시 `targetNoteId`가 있는 기존 노트 제목에 `[Cluster Test]`처럼 대괄호가 포함되면 새 노트 생성 링크로 보이던 문제를 수정했다. `/notes`와 `/graph` 수락 경로는 `targetNoteId`로 찾은 현재 노트 제목을 우선 사용하고, 위키링크 파서는 `]]` 닫힘만 금지해 단일 `[`/`]`가 포함된 target title을 허용한다. `[[제목|별칭]]`은 target title만 추출하도록 보정해 alias가 기존 노트 resolve를 방해하지 않게 했다. API/SSOT/DB schema 변경은 없다.

아티팩트: `../../brainx-next/lib/link-suggestions.ts`, `../../brainx-next/lib/wiki-links.ts`, `../../brainx-next/components/notes/NoteEditor.tsx`, `../../brainx-next/components/notes/WikiLinkNode.tsx`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/components/graph-screen.tsx`.

검증: `node --test lib/wiki-links.test.ts lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과.

## 2026-07-09 06:42 - 위키링크 prefix 닫힘 방어 보정

완료: 대괄호 제목 지원을 위해 `ensureWikiLinkPresent()`의 prefix match가 넓어지면서 `[[JavaScript`를 `Java` 제목의 미완성 링크로 오인해 `[[Java]]Script`처럼 고칠 수 있던 리뷰 지적을 반영했다. 미완성 링크 보정은 target title 뒤가 문서 끝, 공백, `]`, `|`, `#`, `<` 같은 안전 delimiter일 때만 수행하고, `[Cluster Test] ...`처럼 `]`를 포함한 기존 노트 제목은 계속 닫을 수 있게 했다.

아티팩트: `../../brainx-next/lib/wiki-links.ts`, `../../brainx-next/lib/wiki-links.test.ts`.

검증: `node --test lib/wiki-links.test.ts lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 06:55 - 위키링크 rename emoji duplicate 보정

완료: 리뷰에서 지적된 leading emoji 정규화 중복 제목 회귀를 수정했다. 링크 resolve/autocomplete에서는 기존처럼 leading emoji를 무시하되, 제목 rename으로 저장된 raw markdown wiki-link를 치환할 때는 leading emoji를 보존하는 별도 비교 기준을 사용하도록 분리했다. 이에 따라 `Project`와 `📄 Project`가 공존할 때 `📄 Project` rename이 `[[Project]]` 링크를 잘못 변경하지 않는다. API/SSOT/DB schema 변경은 없다.

아티팩트: `../../brainx-next/lib/wiki-links.ts`, `../../brainx-next/lib/wiki-links.test.ts`.

검증: `node --test lib/wiki-links.test.ts lib/link-suggestions.test.ts` 통과. `npm run typecheck` 통과.

## 2026-07-09 16:49 - Graph 시간 효과 토글 추가

완료: `/graph` 우측 상단 레이아웃 컨트롤에 시간 효과 토글과 흐림 강도 슬라이더를 추가했다. 현재 그래프에 로드된 노트들의 최신/최구 `updatedAt` 범위를 기준으로 오래된 노드와 연결선을 상대적으로 흐리게 표시하며, mock seed처럼 `updatedAt` 범위가 없으면 `updated` 상대 라벨을 fallback으로 사용한다. 기존 `전체/최근 1일/최근 1주` 필터는 별도 dim/선택 제한 흐름으로 유지했다. API/SSOT/DB schema 변경은 없다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/components/planet-node.tsx`, `../../brainx-next/components/orbit-edge.tsx`, `../../brainx-next/lib/graph-time-effect.ts`, `../../brainx-next/lib/graph-time-effect.test.ts`.

검증: `npm test` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).
## 2026-07-09 17:35 - Graph 세줄 요약 tooltip 및 Notes 갱신 버튼

완료: 노트 세줄 요약을 `userId + documentGroupId + noteId + markdownHash` 기준으로 DB에 저장하도록 확장하고, `/api/v1/notes/{noteId}/summary` POST 생성 API를 추가했다. 기본 모델은 `BRAINX_NOTE_SUMMARY_MODEL` 기본값 `gpt-5.4-nano`로 고정했으며, 80자 미만 본문은 LLM 호출 없이 `INSUFFICIENT_NOTE_CONTENT`로 실패하게 했다. 노트 저장 이벤트에서는 best-effort 선생성을 요청하고, `/graph` hover에서는 누락 요약을 lazy 생성해 tooltip에 표시한다. `/notes` 우측 컨텍스트 패널에는 저장된 세줄 요약 조회와 `요약 갱신` 버튼을 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/exploration/**`, `src/main/java/com/brainx/intelligence/infrastructure/events/note/**`, `src/main/resources/db/migration/V20260709_01__extend_note_summaries_for_ai_generation.sql`, `scripts/capture_note_summary_quality_cli.py`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/lib/intelligence-api.ts`, `../../contracts-v2/brainx-openapi.ssot.yaml`.

검증: `.\gradlew.bat --no-daemon check --console=plain` 통과. `npm run typecheck` 통과. `python -m py_compile scripts\capture_note_summary_quality_cli.py` 통과. Commerce feature label 관련 테스트 통과. OpenAPI 추출 및 frontend generated type 재생성 완료.

## 2026-07-09 18:07 - Note summary legacy row 조회 보정

완료: 세줄 요약 저장 구조가 `documentGroupId` scoped `summary_id`를 쓰도록 확장되면서 기존 legacy `userId::noteId` row와 새 document-group row가 동시에 존재할 수 있는 JPA 조회 회귀를 수정했다. 기존 `findByUserIdAndNoteId`는 여러 row에서 `Optional` 단건 조회 예외가 나지 않도록 명시적 최신 1건 JPQL + `PageRequest`로 변경했고, `generatedAt null` legacy row는 최신 생성 row보다 뒤로 정렬되게 했다. legacy row와 document-group row가 같이 있는 회귀 테스트를 추가했다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration/NoteSummaryJpaRepository.java`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration/ExplorationJpaAdapter.java`, `src/test/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration/ExplorationJpaAdapterTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.infrastructure.persistence.jpa.exploration.ExplorationJpaAdapterTest --console=plain` 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 18:45 - PR #246 note summary 리뷰 반영

완료: PR #246 미해결 리뷰를 반영했다. `NoteSummary`는 `documentGroupId`, `markdownHash`, `modelId`를 markdown text normalizer로 훼손하지 않고 trim만 수행한다. 노트 content save/delete와 folder delete cleanup은 같은 `userId + noteId`의 다른 Workspace summary를 지우지 않도록 `documentGroupId` scoped delete를 사용한다. `/graph` hover summary API 실패 상태는 같은 hover 동안 terminal로 취급해 반복 호출을 막았다. `/notes`의 `요약 갱신`은 현재 editor HTML을 먼저 저장한 뒤 force summary를 생성한다. 생성 중 local note와 server-resolved note가 동시에 있으면 하나로 병합한다.

아티팩트: `src/main/java/com/brainx/intelligence/exploration/domain/NoteSummary.java`, `src/main/java/com/brainx/intelligence/exploration/application/port/outbound/NoteSummaryPort.java`, `src/main/java/com/brainx/intelligence/infrastructure/events/**`, `src/main/java/com/brainx/intelligence/infrastructure/persistence/jpa/exploration/**`, `../../brainx-next/components/graph-screen.tsx`, `../../brainx-next/components/notes/NotesWorkspace.tsx`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/lib/notes/noteCreationState.ts`.

검증: `.\gradlew.bat --no-daemon test --tests "*ExplorationDomainTest" --tests "*ExplorationJpaAdapterTest" --tests "*WorkspaceNoteEventHandlerTest" --tests "*FolderEventHandlerTest" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `node --test lib/notes/noteCreationState.test.ts` 통과. `npm run typecheck` 통과.

## 2026-07-09 18:51 - Graph 시간 흐림 조회 시점 기준 전환

완료: `/notes`에서 활성 노트가 바뀔 때 사용자/Workspace scope의 localStorage 조회 이력을 기록하고, `/graph` 시간 효과가 해당 조회 시각을 기준으로 opacity를 계산하도록 변경했다. 조회 이력이 일부만 있으면 미조회 노트는 최소 opacity로 표시하고, 조회 이력이 전혀 없으면 기존 `updatedAt`/상대 라벨 fallback을 유지한다. 백엔드/API/SSOT 계약 변경은 없다.

아티팩트: `../../brainx-next/lib/notes/note-view-history.ts`, `../../brainx-next/lib/notes/note-view-history.test.ts`, `../../brainx-next/lib/graph-time-effect.ts`, `../../brainx-next/lib/graph-time-effect.test.ts`, `../../brainx-next/components/notes/NotesWorkspace.tsx`, `../../brainx-next/components/graph-screen.tsx`, `../../README.md`.

검증: `npm test` 통과. `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).
## 2026-07-09 20:04 - Semantic search 저점수 결과 필터링

완료: `/api/v1/intelligence/semantic-search`의 vector 후보에 서버 기본 `brainx.semantic-search.min-score=0.35` 컷을 적용했다. `SEMANTIC` 결과와 `HYBRID`의 semantic 후보만 필터링하고, keyword 후보와 `KEYWORD` mode는 기존 동작을 유지한다. 운영에서는 `BRAINX_SEMANTIC_SEARCH_MIN_SCORE`로 `0.0~1.0` 범위에서 조정할 수 있다. OpenAPI/AsyncAPI 계약 변경은 없다.

아티팩트: `src/main/java/com/brainx/intelligence/exploration/application/usecase/ExplorationService.java`, `src/main/java/com/brainx/intelligence/exploration/application/usecase/SemanticSearchProperties.java`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/exploration/application/usecase/ExplorationServiceTest.java`, `../../README.md`.

검증: `.\gradlew.bat --no-daemon test --tests com.brainx.intelligence.exploration.application.usecase.ExplorationServiceTest --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-09 20:42 - 선택 텍스트 기반 AI 개요 노트 생성

완료: `/notes` 편집기 선택 버블에 `개요 노트 만들기` 액션을 추가했다. 선택 텍스트와 앞뒤 문맥을 기존 inline assist `DRAFT` 요청으로 보내 새 노트 본문용 개요를 생성하고, 생성된 본문을 Workspace 노트로 저장한 뒤 새 탭으로 연다. 새 노트 생성이 성공하면 원본 선택 영역이 그대로 남아 있는 경우 해당 텍스트를 실제 생성 제목 기준 `wikiLink` 노드로 교체하고 원본 노트를 저장한다. 공개 API/SSOT/DB/env 변경은 없다.

아티팩트: `../../README.md`, `../../brainx-next/components/notes/NoteEditor.tsx`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/components/notes/NotesWorkspace.tsx`, `src/main/java/com/brainx/intelligence/assist/application/usecase/AssistService.java`, `src/test/java/com/brainx/intelligence/assist/application/usecase/AssistServiceTest.java`.

검증: `npm run typecheck` 통과. `.\gradlew.bat --no-daemon test --tests "*AssistServiceTest" --console=plain` 통과. `.\gradlew.bat --no-daemon check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).

### 리뷰 반영

- 원본 선택 영역을 wikiLink로 바꾼 뒤 서버 저장이 실패했는데도 성공 메시지가 표시될 수 있던 상태 처리를 수정했다. `linked=true`는 `saveActiveNoteToBackend()` 성공 후에만 설정한다.

## 2026-07-10 02:54 - 선택 AI 요청의 활성 노트 검증

완료: `/notes`의 `RightSidebar`가 선택 텍스트 기반 pending AI 요청을 처리하기 전에 요청의 `noteId`와 현재 `activeNote.id`를 비교하도록 보정했다. 활성 노트가 없거나 요청 원본과 다르면 pending 상태만 정리하고 채팅 메시지 추가, inline assist 호출, 개요 노트 생성을 모두 건너뛴다. OpenAPI/AsyncAPI SSOT 변경은 없다.

아티팩트: `../../brainx-next/components/notes/RightSidebar.tsx`, `../../README.md`.

검증: `npm test` 통과(84 tests). `npm run typecheck` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력).
## 2026-07-10 03:20 - 관리자 사용자 최근 활동 정렬

완료: `brainx-admin-next` 사용자 관리 목록이 API의 원본 `lastActiveAt`을 보존하고, `최근 활동` 헤더 클릭으로 최신순(기본값)/오래된순을 전환하도록 구현했다. 활동 시각이 없거나 유효하지 않은 사용자는 양 방향 모두 마지막에 두고, 동일 시각은 사용자 ID로 안정 정렬한다. 헤더에는 방향 아이콘, 키보드 포커스, `aria-sort`를 적용했으며 기존 검색/필터/행 선택 상태는 파생 정렬 결과와 함께 유지한다. 서버 정렬 query parameter와 AsyncAPI 이벤트는 추가하지 않았다.

아티팩트: `../../brainx-admin-next/components/admin-console.tsx`, `../../brainx-admin-next/app/globals.css`, `../../brainx-admin-next/lib/admin-data.ts`, `../../brainx-admin-next/lib/admin-api.ts`, `../../brainx-admin-next/lib/admin-mock.ts`, `../../contracts-v2/brainx-openapi.ssot.yaml`, `../../README.md`.

검증: `npm run typecheck` 통과. `npm run build` 통과(기존 Turbopack workspace root 경고만 출력). mock API 사용자 14명의 최신순/오래된순과 null-last 순서 검증 통과. `npx --yes yaml valid`로 OpenAPI/AsyncAPI SSOT YAML 검증 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 인앱 브라우저는 현재 세션에 연결 가능한 브라우저가 없어 클릭 기반 시각 검증을 실행하지 못했다.

### 리뷰 반영 (03:30)

- Firefox 111~114에서도 사용자 목록이 렌더링되도록 `filter()`가 만든 새 배열에는 `toSorted()` 대신 호환 가능한 `sort()`를 적용했다.
- mock 사용자 세션을 `lastActiveAt`에서 먼저 만들고 `lastLogin`을 최신 세션에서 파생하도록 통일했다. 활동 시각이 없는 사용자는 `lastLogin=null`, `sessions=[]`를 함께 반환한다.
- `npm run typecheck`, `npm run build`를 다시 통과했고, mock 사용자 14명의 `lastLogin`/`sessions` 일관성과 활동 없음 사용자의 sessionless 응답을 검증했다.
## 2026-07-10 03:38 - 웹 검색 기본 모델 gpt-5.4-mini 전환

완료: `ExternalSearchProperties`, `application.yaml`, AWS dev Compose, Kubernetes ConfigMap, Helm 설계의 `OPENAI_WEB_SEARCH_MODEL` 기본값을 `gpt-5.4-mini`로 통일했다. 명시된 환경변수 override는 계속 우선하며 이를 configuration test로 고정했다. 외부 검색 문서, AI 기능 카탈로그, 서비스 README에는 검색 결과의 `answer`와 `sources`가 최종 채팅 답변 LLM context로 전달되어 사용자 문체, 대화/노트/클라이언트 context, route 규칙, assistant message의 `webSources` 저장 흐름이 유지됨을 반영했다. 공개 API와 SSE schema가 바뀌지 않아 OpenAPI/AsyncAPI SSOT는 수정하지 않았다.

아티팩트: `src/main/java/com/brainx/intelligence/infrastructure/search/ExternalSearchProperties.java`, `src/main/resources/application.yaml`, `src/test/java/com/brainx/intelligence/infrastructure/search/ExternalSearchConfigurationTest.java`, `docs/technical/external-search.md`, `docs/technical/ai-feature-catalog.md`, `README.md`, `../../infra/aws-dev/deploy/docker-compose.yml`, `../../k8s/apps/intelligence-service-configmap.yaml`, `../../k8s/helm/HELM_DESIGN.md`.

검증: `OpenAiExternalSearchAdapterTest`, `ExternalSearchConfigurationTest`, `ChatServiceTest` 묶음 통과. `.\gradlew.bat --no-daemon clean check --console=plain` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). 실제 provider smoke는 첫 실행에서 로컬 Kafka/Eureka 부재로 usage event 발행이 지연되어 실패했으나, `BRAINX_EVENTS_PRODUCER_ENABLED=false`, `BRAINX_EVENTS_CONSUMER_ENABLED=false`, `EUREKA_CLIENT_ENABLED=false`로 외부 인프라를 끈 재실행이 통과했다. `gpt-5.4-mini` Responses `web_search`에서 출처 8개, stream event 5개, completed event와 response id를 확인했으며 결과는 `build/external-search-captures/20260710-gpt-5-4-mini-web-search-smoke-retry/summary.json`에 저장했다.

## 2026-07-10 03:54 - 관리자 공지 발송 모달 닫기 개선

완료: `brainx-admin-next`의 공지 발송 모달이 idle 상태에서 backdrop 클릭으로 닫히고 작성값을 폐기하도록 했으며, 내부 콘텐츠 클릭은 전파를 막아 모달을 유지한다. 발송 중에는 기존 취소·중복 발송 차단과 함께 backdrop 닫기도 막는다. `SEND_NOTICE` 성공 시 보존한 수신 인원으로 사용자 목록 재조회 없이 모달 닫기, 선택 해제, 성공 안내를 즉시 처리하며 요청 실패 시에는 모달과 작성값을 유지해 재시도할 수 있다. HTTP schema와 AsyncAPI 이벤트는 변경하지 않고 OpenAPI operation 설명과 root README에 UI 정책만 기록했다.

아티팩트: `../../brainx-admin-next/components/admin-console.tsx`, `../../contracts-v2/brainx-openapi.ssot.yaml`, `../../README.md`.

검증: `npm run typecheck` 통과. `npm run build` 통과(기존 Turbopack workspace root 경고만 출력). `npx --yes yaml valid contracts-v2\brainx-openapi.ssot.yaml contracts-v2\brainx-asyncapi.ssot.yaml` 통과. `git diff --check` 통과(LF/CRLF 경고만 출력). `ADMIN_MOCK_ENABLED=true` mock 개발 서버의 `/login` 응답과 기동은 확인했지만, 현재 세션에 연결 가능한 인앱 브라우저가 없어 클릭 기반 모달 동작 검증은 실행하지 못했다.
## 2026-07-10 04:57 - 클러스터링 리뷰 지적사항 수정

완료: 징검다리 노트 projection 생성 시 nullable `created.folderId` 때문에 프런트엔드 타입 검사가 실패하던 문제를 `created.folderId ?? cluster` fallback으로 수정했다. 증분 클러스터링은 최초 entitlement 검사를 cluster job 저장과 `ClusterJobRequested` 발행 전에 수행하도록 이동해, 거부 시 `FAILED` job으로 흡수하지 않고 기존 `ClusteringForbiddenException`/HTTP 403 경계를 유지한다. 해당 경로에서 job·AI 호출·이벤트가 추가되지 않는 회귀 테스트를 추가했다.

아티팩트: `../../brainx-next/components/graph-screen.tsx`, `src/main/java/com/brainx/intelligence/clustering/application/usecase/ClusteringService.java`, `src/test/java/com/brainx/intelligence/clustering/application/usecase/ClusteringServiceTest.java`.

검증: `.\gradlew.bat --no-daemon test --tests "com.brainx.intelligence.clustering.application.usecase.ClusteringServiceTest" --console=plain`, `.\gradlew.bat --no-daemon check --console=plain`, `npm run typecheck`, `git diff --check` 통과. 최초 전체 `.\gradlew.bat --no-daemon clean check --console=plain`은 실패 로그 없이 124초 실행 제한으로 종료되어, 생성된 산출물을 재사용한 `check` 성공으로 최종 확인했다. 공개 API 계약은 변경하지 않아 OpenAPI/AsyncAPI extraction과 frontend codegen은 생략했다.

## 2026-07-10 05:12 - AI 개요 노트 확인 및 비자동 이동 흐름

완료: `/notes` 선택 버블의 `개요 노트 만들기`를 인라인 AI 패널에서 분리해 Workspace 레벨 확인 모달로 전환했다. 확인 후 모달 안에서 로딩과 오류를 표시하고, 약 300자 기준의 `개요` bullet과 `향후 작성할 내용` 체크리스트만 생성한다. 생성 노트는 목록과 그래프에 반영하되 활성 탭이나 URL을 바꾸지 않으며, 원본 선택 텍스트를 실제 생성 제목의 `[[WikiLink]]`로 치환해 사용자가 링크를 눌렀을 때만 이동한다. 공용 `ConfirmDialog`에는 dialog semantics, 포커스 트랩·복귀, 키보드 닫기, 로딩 중 닫기·중복 실행 방지, 인라인 오류 안내를 추가했다. 공개 API와 이벤트 계약은 변경하지 않아 OpenAPI/AsyncAPI SSOT는 수정하지 않았다.

아티팩트: `../../brainx-next/components/notes/ConfirmDialog.tsx`, `../../brainx-next/components/notes/NotesWorkspace.tsx`, `../../brainx-next/components/notes/RightSidebar.tsx`, `../../brainx-next/lib/notes/ai-outline-note.ts`, `../../brainx-next/lib/notes/ai-outline-note.test.ts`, `../../README.md`.

검증: `npm run typecheck`, `npm test` 통과(최종 실행 기준 102 tests), `npm run build` 통과. 로컬 프로덕션 서버의 `/notes`에서 텍스트 선택 후 확인 모달이 인라인 AI를 열지 않고 표시되는지 확인했고, 라이트·다크 모드 레이아웃과 대비, 초기 취소 버튼 포커스, Escape 닫기와 편집기 포커스 복귀를 검증했다. `git diff --check` 통과(LF/CRLF 경고만 출력).

## 2026-07-10 05:17 - 노트 편집 중 탐색기 순서 고정

완료: `/notes` 탐색기의 기본 `최근 수정순`에서 활성 노트 본문을 편집하거나 AI 연결 제안을 수락해 `updatedAt`이 갱신돼도 같은 폴더 안의 행이 즉시 이동하지 않도록 정렬 전용 수정 시각을 고정했다. 실제 저장 시각은 그대로 유지하며, 다른 노트를 열거나 정렬 기준·방향을 명시적으로 바꾸면 최신 값으로 다시 정렬한다. 폴더 트리, 루트 노트, 검색 결과, 즐겨찾기가 같은 정렬 override를 공유한다. 공개 API/이벤트 계약은 변경하지 않았고, `connection-api.md`의 `documentGroupId` 설명만 현재 SSOT에 맞췄다.

아티팩트: `../../brainx-next/components/notes/NotesExplorer.tsx`, `../../brainx-next/components/notes/FolderTree.tsx`, `../../brainx-next/lib/notes/noteTypes.ts`, `../../brainx-next/lib/notes/noteTypes.test.ts`, `docs/technical/connection-api.md`.

검증: `npm test` 통과(102 tests). `git diff --check` 통과. `npm run typecheck`는 이번 변경과 무관한 기존 미커밋 `../../brainx-next/lib/intelligence-api.ts`의 누락 심볼 `usageLimitReasonFrom`, `AiUsageLimitExceededError` 2건 때문에 실패했으며, 이번 변경 파일에서는 타입 오류가 보고되지 않았다. 공개 계약 변경이 없어 OpenAPI/AsyncAPI extraction과 frontend codegen은 생략했다.

## 2026-07-10 05:18 - AI 채팅 초안 노트 질문형 제목 방지

완료: `/chat`의 `COMPOSE`·`NOTE_ACTION` 응답을 Workspace note로 저장할 때 AI 응답 heading이 없으면 질문형 chat thread 제목을 그대로 사용하던 fallback을 제거했다. AI prompt는 첫 heading을 2~8어절의 주제형 명사구로 요구하며 질문·요청 반복과 의문 표현을 금지한다. 프런트엔드는 비질문형 Markdown heading, 의미 있는 비질문형 본문 줄 순서로 제목을 선택하고 적합한 후보가 없으면 `AI 초안`을 사용한다. Agent `CREATE_NOTE`와 공개 API 계약은 변경하지 않았다.

아티팩트: `src/main/java/com/brainx/intelligence/chat/application/usecase/ChatService.java`, `src/test/java/com/brainx/intelligence/chat/application/usecase/ChatServiceTest.java`, `docs/technical/rag-chat-api-frontend-integration.md`, `README.md`, `../../brainx-next/components/chat-screen.tsx`, `../../brainx-next/components/chat/chat-draft-utils.ts`, `../../brainx-next/components/chat/chat-draft-title.ts`, `../../brainx-next/components/chat/chat-draft-title.test.ts`.

검증: `ChatServiceTest`, `node --test components/chat/chat-draft-title.test.ts`, `npm run typecheck`, `npm test`(102 tests), `git diff --check` 통과. Intelligence 전체 `check`는 동시에 진행 중인 다른 미커밋 변경의 Insight·Organization·Settings·Architecture 테스트 14건 때문에 실패했으며, 이번 변경의 `ChatServiceTest`는 통과했다. 공개 계약 변경이 없어 OpenAPI/AsyncAPI extraction과 frontend codegen은 생략했다.

## 2026-07-10 05:22 - 403 quota 거절 시 로그인 세션 유지

완료: 프런트 API 클라이언트가 `401 Unauthorized`와 `403 Forbidden`을 모두 세션 만료로 처리하던 문제를 수정했다. 공통 `isAuthSessionFailureStatus` 판별을 도입해 `401`에서만 access token 갱신과 로컬 세션 무효화를 수행하고, `403`은 권한·capability·quota 오류로 기존 세션을 유지한 채 응답 본문을 화면에 전달한다. Intelligence REST/SSE의 `MONTHLY_CREDIT_LIMIT_EXCEEDED`와 `GUEST_AI_CALL_LIMIT_EXCEEDED`는 공통 변환기를 통해 `AiUsageLimitExceededError`로 유지했다. Workspace 게스트 세션 최초 발급을 위한 `401/403` 1회 재시도는 보존했다. SSOT와 서비스 README에 상태 코드 처리 원칙을 기록했다.

아티팩트: `../../brainx-next/lib/auth-http-status.ts`, `../../brainx-next/lib/ai-usage-limit-error.ts`, `../../brainx-next/lib/*-api.ts`, `../../contracts-v2/brainx-openapi.ssot.yaml`, `README.md`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`.

검증: `npm test` 통과(102 tests), `npm run typecheck` 통과, `uv run --with PyYAML python scripts\extract_intelligence_openapi.py` 통과(48 operations), `.\gradlew.bat --no-daemon check --console=plain` 통과, `git diff --check` 통과. 최초 `.\gradlew.bat --no-daemon clean check --console=plain`은 기존 `build/test-results/test/binary/output.bin` 파일 잠금으로 `clean` 단계에서 실패했으며, 다른 프로세스를 종료하지 않고 `check`로 전체 컴파일·테스트·아키텍처 gate를 검증했다.

## 2026-07-10 05:33 - 전체 변경 통합 검증 및 PR 게시 준비

완료: 현재 `kyj2` 작업 트리의 Intelligence Service, `brainx-next`, 루트 SSOT와 README 변경 전체를 하나의 PR 범위로 확정했다. OpenAPI SSOT와 로컬 Intelligence 추출본을 다시 동기화하고, 변경 파일에 민감정보 파일명이 포함되지 않았음을 확인했다. 이후 전체 변경을 단일 커밋으로 묶고 최신 `origin/main`을 병합해 게시한다.

아티팩트: 현재 작업 트리의 전체 변경 파일, `../../contracts-v2/brainx-openapi.ssot.yaml`, `src/main/resources/contracts/knowledge-intelligence.openapi.yaml`.

검증: `uv run --with PyYAML python scripts\extract_intelligence_openapi.py` 통과(48 operations, 42 paths), `.\gradlew.bat --no-daemon check --console=plain` 통과, `brainx-next`의 `npm run typecheck`, `npm test`(102 tests), `npm run build` 통과, `brainx-admin-next`의 `npm run typecheck`, `npm run build` 통과, `npx --yes yaml valid contracts-v2\brainx-openapi.ssot.yaml contracts-v2\brainx-asyncapi.ssot.yaml` 통과, `git diff --check` 통과. `origin/main` 병합 충돌 해결 후 같은 검증을 다시 수행했다. 프런트 테스트의 `MODULE_TYPELESS_PACKAGE_JSON`, admin workspace root 경고와 desktop installer 미존재 안내는 실패가 아니며 두 build 모두 정상 완료됐다.

## 2026-07-10 05:30 - PR #254 원격 충돌 해결

완료: 로컬 checkout이나 워크트리 수정 없이 GitHub CLI의 원격 Git Data API로 main의 최근 두 커밋을 kyj2에 3-way 병합했다. GitHub unified patch의 19개 hunk를 검증해 이미 반영된 15개는 유지하고, README.md, Intelligence worklog, Admin 공지 모달, OpenAPI 설명의 4개 충돌 구간을 합친 merge commit을 생성했다.

아티팩트: README.md, brainX_back/Intelligence-Service/vaults/worklogs/2026-07.md, brainx-admin-next/components/admin-console.tsx, contracts-v2/brainx-openapi.ssot.yaml, merge commit 334f45d5d52e2b1fb53f2fa0a32814039a59e1e7.

검증: PR #254가 MERGEABLE임을 확인했다. CI는 확인 시점에 ci / detect changes pending이었다. 사용자 지시에 따라 로컬 build와 test는 실행하지 않았다.

## 2026-07-10 05:44 - 개요 노트 AI 사용량 한도 모달 복원

완료: `/notes`의 개요 노트 확인 모달에서 AI 생성 요청이 `AiUsageLimitExceededError`로 실패할 때 오류 문구만 표시하고 로그인·플랜 업그레이드 안내 모달을 열지 않던 리뷰 지적사항을 수정했다. 기존 인라인 AI 경로와 동일하게 `openAiUsageLimitModal(error.reason)`을 호출하면서 확인 dialog의 오류 안내도 유지한다. 공개 API와 이벤트 계약은 변경하지 않았다.

아티팩트: `../../brainx-next/components/notes/NotesWorkspace.tsx`.

검증: `npm run typecheck`, `npm test` 통과(102 tests), `git diff --check` 통과.
## 2026-07-10 06:04 - Intelligence Service 문서 라우팅·계약 정합화

완료: `agent-documentation-compounder` 기준으로 root guide는 짧게 유지하고 worklog workflow를 추가했다. 중복된 `vaults/INDEX.md` 라우팅을 단일 annotated index로 정리하고, 사람용 `docs/README.md`와 LLM 품질 상세 평가 index를 만들었다. README의 SSOT/추출본 관계와 public API 34개를 local provider contract에 맞추고, `documentGroupId`, 소비 이벤트 13개 구현 범위, producer default, historical quality report의 오래된 설명을 현재 구현에 맞게 보정했다. active 문서의 Python script 예시는 모두 `uv` 실행으로 통일했다. OpenAPI/AsyncAPI SSOT와 Java 코드는 변경하지 않았다.

아티팩트: `AGENTS.md`, `README.md`, `docs/README.md`, `docs/domain/README.md`, `docs/technical/README.md`, `docs/technical/llm-quality-evaluations/README.md`, `docs/technical/consumed-events-implementation-checkpoints.md`, `vaults/INDEX.md`, `vaults/workflows/worklog.md`, `vaults/workflows/verification.md`, `vaults/durable/INDEX.md` 및 관련 기술 문서.

검증: 문서 전용 변경이라 Gradle test는 생략했다. local Markdown link 69개 파일 검증 통과, README public API 표와 local provider contract의 34 operations 대조 통과, active 문서 bare `python` command 없음 확인, extraction script `--help` 2종 통과, `git diff --check` 통과(LF/CRLF 변환 warning만 출력).
