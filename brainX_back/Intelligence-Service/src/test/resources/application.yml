spring:
  application:
    name: intelligence-service
  jpa:
    open-in-view: false

brainx:
  jwt:
    secret: test_intelligence_jwt_secret_at_least_32_bytes
  service-token: test_intelligence_service_token_at_least_32_bytes

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
